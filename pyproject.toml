[project]
name = "godmode-project"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8",
    "numpy",
]

[dependency-groups]
dev = [
    "black",
    "pyright",
    "ruff",
]

[tool.uv]
package = false
