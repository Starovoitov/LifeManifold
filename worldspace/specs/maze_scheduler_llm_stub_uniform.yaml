schema_version: "maze-1.0"
# RQ1 maze Phase B: stub × uniform_frontier. Empty archive, 5k slots.
# Protocol: artifacts/Q1_RQ1_SECOND_DOMAIN.md
# Does not amend Holm H5. Do not reuse maze_scheduler_llm_stub.yaml (650 iters).
condition: llm_stub_uniform
target_selection: uniform_frontier
iterations: 100
batch_size: 50
archive_resolution: 30
initial_random_candidates: 100
llm_prompt_mode: stub
surrogate_checkpoint: null
acquisition:
  mode: off
