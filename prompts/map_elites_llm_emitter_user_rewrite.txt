Target niche: stability ≈ {target_stability:.2f} (±0.03), diversity ≈ {target_diversity:.2f} (±0.03)

You previously proposed a draft WorldSpec for this niche. A surrogate model scored that draft
(not the parent). The draft predicted fitness and uncertainty below are surrogate estimates,
not ground-truth simulation outcomes. Rewrite the draft using those estimates while staying
in-niche.

Parent true fitness (archive, already simulated): {parent_true_fitness:.3f}
Draft predicted fitness ≈ {child_surrogate_mean:.3f}, uncertainty = {child_surrogate_uncertainty:.3f}

Draft WorldSpec to rewrite:
{draft_world_spec_json}

Current best elite in this cell (context; do not copy blindly):
{current_elite_json}

Examples of successful elites from nearby niches (with fitness):
{few_shot_examples}

Rewrite rules:
1. Start from the draft above; make compact named-field edits (rules/scalars).
2. If draft predicted fitness < parent true fitness − 0.05, prioritize edits that raise
   predicted fitness; otherwise preserve draft structure.
3. Must land in the target stability/diversity niche.
4. If uncertainty is high relative to typical values (roughly >= 0.30), change at most
   1–2 fields.
5. Return the identical draft if predicted fitness is already high and uncertainty is low;
   otherwise edit.

Constraints for world_spec:
{constraints}

Return JSON only:
{{
  "world_spec": {{ ... full WorldSpec without the seed field ... }}
}}
