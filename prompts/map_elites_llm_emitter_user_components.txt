Target niche: stability ≈ {target_stability:.2f} (±0.03), diversity ≈ {target_diversity:.2f} (±0.03)

Surrogate prediction for the parent world (Strategy-A components):
  stability: {surrogate_stability:.3f}
  diversity: {surrogate_diversity:.3f}
  oscillation_score: {surrogate_oscillation:.3f}
  topology_interface_index: {surrogate_topology_interface:.3f}
  topology_window_heterogeneity: {surrogate_topology_heterogeneity:.3f}
  final_density: {surrogate_final_density:.3f}
  early_extinction_prob: {surrogate_early_extinction_prob:.3f}
  composed fitness: {surrogate_mean:.3f}
  uncertainty: {surrogate_uncertainty:.3f}

Guidance from surrogate:
- If early_extinction_prob >= 0.5, favor survival-friendly rules (higher final_density; avoid early wipeout).
- If uncertainty >= 0.7, prefer small mutations relative to the parent.
- Use stability and diversity components jointly with the target niche; do not optimize composed fitness alone.

Current best elite in this cell:
{current_elite_json}

Examples of successful elites from nearby niches (with fitness):
{few_shot_examples}

Generate a new WorldSpec that:
1. Lands in the target niche for stability and diversity.
2. Maximizes fitness per the formula above (avoid early extinction).
3. Differs from existing elites.
4. Accounts for high surrogate uncertainty.

Constraints for world_spec:
{constraints}

Return JSON only:
{{
  "world_spec": {{ ... full WorldSpec without the seed field ... }}
}}
