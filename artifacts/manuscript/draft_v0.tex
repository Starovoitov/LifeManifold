\documentclass[preprint,12pt]{elsarticle}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tabularx}
\usepackage{microtype}
\usepackage[section]{placeins} % FloatBarrier before every \section (stops appendix floats crossing letter boundaries)
\usepackage{hyperref}
\hypersetup{hidelinks}

\graphicspath{{figures/}}

% Allow floats near their call sites (default [t]-only queues them to the end).
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\textfraction}{0.07}
\renewcommand{\floatpagefraction}{0.65}
\setcounter{topnumber}{4}
\setcounter{bottomnumber}{3}
\setcounter{totalnumber}{6}

% Soften overfull lines from bold / monospace runs without ragged paragraphs.
\emergencystretch=2.5em
\tolerance=1500

% Center a tabular; scale down only if it would overflow the text width.
\newsavebox{\wsfitbox}
\newcommand{\fitcenter}[1]{%
  \sbox{\wsfitbox}{#1}%
  \centering
  \ifdim\wd\wsfitbox>\linewidth
    \resizebox{\linewidth}{!}{\usebox{\wsfitbox}}%
  \else
    \usebox{\wsfitbox}%
  \fi
}

% Wide diagrams (Fig.~1 / Fig.~3): prefer full text width; taller print budget for dense flowcharts.
\newcommand{\pagefitfig}[1]{%
  \includegraphics[width=\linewidth,height=0.62\textheight,keepaspectratio]{#1}%
}

\journal{Swarm and Evolutionary Computation}

% Fill these at journal submission (after tagging the release commit on GitHub).
\newcommand{\SubmissionGitTag}{journal-v1}
\newcommand{\SubmissionGitShort}{TBD} % e.g. 7607364b4d31 — replace with `git rev-parse --short=12 HEAD` of the tagged commit
\newcommand{\SubmissionGitFull}{TBD}  % full 40-char SHA of the same commit

% Drop elsarticle "Preprint submitted to …" footer (Elsevier EM preference).
\makeatletter
\def\ps@pprintTitle{%
  \let\@oddhead\@empty
  \let\@evenhead\@empty
  \def\@oddfoot{\reset@font\hfil\thepage\hfil}%
  \let\@evenfoot\@oddfoot
}
\makeatother

\begin{document}

\begin{frontmatter}

\title{Asymmetric Surrogate Value in {MAP}-{E}lites: Attachment-Point Decomposition under {LLM} Variation}

\author{Artem Starovoitov\corref{cor1}}
\ead{artem.starovoitov@proton.me}
\ead[url]{https://orcid.org/0009-0005-3170-4865}
\address{Independent Engineer, Moscow, Russia}
\cortext[cor1]{Corresponding author}

\begin{abstract}
Classical surrogate-assisted evolutionary algorithms ask where a surrogate should intervene in the propose$\rightarrow$evaluate loop (individual- vs.\ generation-based evolution control). LLM-guided MAP-Elites raises a structurally related question: should a shared surrogate attach \emph{before} generation (prompt-side advice) or \emph{after} generation (evaluation gating)? We isolate those intervention points with checkpoint, archive, budget, and emitters held fixed. After-generation attachment improves evaluation-count efficiency on our testbed (${\sim}{+}3.7$~pp coverage per simulation at mid-budget; $33.5\%$ fewer sims at fixed iterations) but, on this cheap evaluator, finishes $-0.83$~pp at the same iteration schedule and is slower in wall time (${\sim}20$ vs.\ ${\sim}7$~min). Matched before-generation scalars stay within $\pm 2$~pp of stub constants on \texttt{qwen-turbo} ($n{=}10$; TOST). The LLM is one concrete emitter; CMA-ME (H4) and maze transfer (H5) bound the same methodological contrast. The contribution is an empirical model-management result for MAP-Elites: matched before-generation soft advice is flat here, while after-generation hard gating improves evaluation-count efficiency---positive but conditional on the cheap-evaluator wall and terminal trade-offs above.
\end{abstract}

\begin{keyword}
evolutionary computation \sep quality-diversity \sep MAP-Elites \sep surrogate-assisted evolutionary algorithms \sep large language models \sep open-ended search
\end{keyword}

\end{frontmatter}

\section{Introduction}
\label{sec:intro}

\textbf{One scientific question.} Classical SAEA model management asks where a surrogate should intervene before an expensive evaluation~\cite{jin2005fitness}. LLM-guided MAP-Elites reopens that question with a new soft channel: should a shared surrogate attach \emph{before} generation (prompt-side advice) or \emph{after} generation (evaluation gating)? We hold checkpoint, archive, budget, and emitters fixed so the answer is about \emph{channel}, not a new pipeline. The non-obvious result is an asymmetry: hard after-generation gating reallocates evaluations; soft before-generation scalars stay within $\pm 2$~pp. The LLM is one concrete emitter; CMA-ME (H4) and maze transfer (H5) bound that reading.

Quality-diversity (QD) maintains archives of solutions that are both high-performing and behaviourally diverse. MAP-Elites assigns each candidate to a behavioural niche and keeps the best elite per cell~\cite{mouret2015illuminating}. Every candidate still needs a black-box evaluation. When that roll-out is costly, evaluation count dominates wall time, and surrogate-assisted illumination---a QD instance of classical SAEA model management~\cite{jin2005fitness}---becomes attractive~\cite{gaier2018sail}.

The scientific object is that EC model-management question, not an LLM product. An LLM-guided stack still couples emitters, a simulator, and an archive. A surrogate is a natural place to inject feedback into that loop. Pipelines usually change several of those pieces at once; we change only the surrogate intervention point.

Previous work often compared complete \emph{pipelines}. We compare \textbf{intervention points} (Figure~\ref{fig:pipeline}). Payoffs are measured on a controlled black-box evaluator (\S\ref{sec:substrate}). Labels \textbf{H1}--\textbf{H5} name contrasts that probe one methodological model; statuses are Table~\ref{tab:h-status} only.

\subsection{Contributions}

\begin{enumerate}
  \item \textbf{Soft/hard channel asymmetry (what is new).} With one shared checkpoint, matched archive policy, and matched budgets, after-generation hard gating improves coverage \emph{per simulation} (${\sim}{+}3.7$~pp at mid-budget; $33.5\%$ fewer sims at fixed iterations; Figure~\ref{fig:acquisition}; \S\ref{sec:h2}), while matched before-generation soft scalars stay within $\pm 2$~pp of stub constants on \texttt{qwen-turbo} ($n{=}10$; TOST; \S\ref{sec:decomposition}). That asymmetry is the contribution: ``prompt the LLM'' and ``gate evaluations'' are not interchangeable uses of the same surrogate. On this cheap evaluator the hard gate still finishes $-0.83$~pp at fixed iterations and is slower in wall time (${\sim}20$ vs.\ ${\sim}7$~min)---so the win is evaluation-indexed, not a free terminal/wall upgrade.
  \item \textbf{Measurement design that isolates the channels.} We hold checkpoint, emitters, archive, and budget fixed and vary only the intervention point (Figure~\ref{fig:pipeline}; Table~\ref{tab:h-status}). This is an experimental decomposition of classical evolution-control concerns~\cite{jin2005fitness} for LLM-guided MAP-Elites, not a claim that we generalize the full individual-/generation-based SAEA taxonomy (\S\ref{sec:related}). Soft before-generation has no direct SAIL analogue; hard after-generation is individual-based pre-selection.
  \item \textbf{Confounds and bounds.} The historical $+15$~pp stub$\rightarrow$hints PASS is mostly archive policy (${\approx}{+}14.8$~pp), not live prompt scalars. Continuous-relaxation CMA-ME, maze transfer (H5), and LLM$+$filter stacking (H3-prod \textbf{blocked}; H3-gray \textbf{NI-pass-not-superiority}; Table~\ref{tab:h-status}) bound where the asymmetry does and does not extend.
\end{enumerate}

What the SAEA/QD reader should take away is therefore not a new name for ``prompt vs.\ gate,'' but an empirically grounded channel ranking under matched controls: hard after-generation reallocates simulations; soft before-generation does not move terminal coverage here. Provider replications, CMA-ME, and maze transfer bound that answer---they are not parallel research lines. Section~\ref{sec:method} defines the loop; \S\ref{sec:protocol} the design; \S\ref{sec:results} the evidence; \S\ref{sec:discussion} the reading. Scope and threats: \S\ref{sec:limitations}.

\section{Background and related work}
\label{sec:related}

\subsection{Quality-diversity and MAP-Elites}

Quality-diversity (QD) algorithms seek collections of high-performing yet behaviourally diverse solutions rather than a single optimum~\cite{pugh2016quality,cully2018quality,chatzilygeroudis2021qd}. MAP-Elites maintains an archive partitioned by user-defined behavioural characteristics (BC) and retains the best elite per niche~\cite{mouret2015illuminating}. Heterogeneous emitter sets can improve illumination efficiency relative to a single variation operator~\cite{cully2020multiemitter}; our confirmatory stack mixes random, genetic, and LLM slots in one batch.

Continuous variation operators remain important on fixed-length genomes. Covariance Matrix Adaptation MAP-Elites (CMA-ME) couples CMA-ES adaptation with MAP-Elites archiving and often dominates random or isotropic mutation on continuous control and design tasks~\cite{fontaine2020cmame}. CMA-MAE extends the idea with MAP-annealing and separate result archives, trading coverage for per-niche quality~\cite{fontaine2023cmamae,zhao2024cmamae}. We implement both through \texttt{ribs} v0.11.0~\cite{pyribs2020} with an adapter that evaluates candidates on the same black-box simulator as the LLM stack (\S\ref{sec:method}). Our pyribs baselines use homogeneous CMA-ES emitter pools rather than mixed pyribs ``cocktails,'' so they represent strong but not maximal pyribs configurations.

\subsection{Surrogate-assisted illumination}

Expensive simulators motivate surrogate models that predict outcomes from static design features~\cite{jin2005fitness,jones1998ego}.
Classical surrogate-assisted evolutionary algorithms (SAEAs) treat that prediction problem as \emph{model management}: when, and how strongly, to trust the approximate fitness instead of the true evaluator~\cite{jin2005fitness}.
Two recurring evolution-control patterns are \emph{individual-based} control (pre-select or filter candidates before expensive evaluation) and \emph{generation-based} control (evaluate only some generations fully, or alternate approximate and exact generations).
We do not claim a full generalization of that taxonomy. The taxonomy supplies vocabulary; the contribution is empirical. Once an LLM emitter is present, the same surrogate can attach as soft before-generation advice or hard after-generation gating. A priori, both channels might help, both might fail, or soft LLM conditioning might dominate. Our matched design shows they do not: hard gating reallocates evaluations; soft scalars stay flat (\S\ref{sec:decomposition}--\ref{sec:h2}).
The after-generation surrogate is individual-based pre-selection: a threshold gate decides whether a proposed genome spends a simulator call.
The before-generation surrogate is a softer use of the same model: scalars enter the LLM prompt and try to shape variation, but never skip \texttt{evaluate\_candidate}.

Surrogate-Assisted Illumination (SAIL) is the canonical QD answer: fit a Gaussian process over the behaviour--fitness landscape, run MAP-Elites on an acquisition surface (classically UCB-style), and spend real evaluations only on a selected subset~\cite{gaier2018sail,gaier2017sail,gaier2017aiaa}.
Deep Surrogate Assisted MAP-Elites (DSA-ME) replaces the GP with neural predictors trained online in an inner loop and validates candidates in an outer game-engine loop~\cite{grillotti2022dsame}.
Related Bayesian QD lines couple acquisition to descriptor and fitness models~\cite{kent2025boqd}.
Discount Model Search continues the surrogate-QD line without LLMs, replacing histogram discounts with a learned continuous discount model for high-dimensional measure spaces~\cite{tjanaka2026dms}.
Across this QD family the surrogate is a \emph{search accelerator}: it can change which candidates receive ground truth, and often \emph{is} the surface being illuminated rather than a side channel into an otherwise unchanged MAP-Elites loop.
SAIL-style acquisition is thus a strong form of individual-based evolution control specialized to behaviour--fitness GPs; our after-generation threshold gate is SAIL-\emph{kin} in that controlling sense, while the before-generation prompt channel has no direct SAIL analogue.

Reported evaluation savings are strongest in computational design.
On two-dimensional airfoil tasks SAIL needs orders-of-magnitude fewer flow simulations than MAP-Elites while filling behaviour bins~\cite{gaier2018sail}; with the budget CMA-ES requires to optimize one bin, SAIL can match that quality in every bin~\cite{gaier2017sail}.
Three-dimensional aerodynamics and high-dimensional discrete deckbuilding show the same qualitative pattern~\cite{gaier2017aiaa,grillotti2022dsame}.
Evidence for stochastic simulators is thinner: these studies assume expensive but largely deterministic evaluators or batched outer validation, not single-rollout archive updates under seeded simulator noise.
Our confirmatory tier holds the simulator budget fixed (32{,}500 evaluations per seed) and asks, for LLM-guided QD, \emph{where} the same surrogate should attach under full-simulation archive updates---not whether a new SAIL-style illuminator brand wins on expensive CFD.

\textbf{Positioning vs.\ SAIL / DSA-ME.}
Our stack differs from those lineages on four points that matter for the before/after framework.
(i)~\emph{Model:} a multi-task MLP ensemble with MC-dropout over genome features (optional LightGBM path), not a monolithic GP fitness surface; offline MLP-vs-GP/UCB checks prefer the production MLP under the runtime compose definition (appendix~\S\ref{sec:surrogate-ablation-appendix}).
(ii)~\emph{Attach points:} the before-generation surrogate \textbf{never skips} \texttt{evaluate\_candidate}---it only supplies scalars to the LLM prompt---while the after-generation surrogate optionally gates with a production \texttt{threshold\_gate} ($\texttt{min\_fit}{=}0.45$, $\texttt{max\_u}{=}1.0$), not SAIL's GP-UCB illuminator.
(iii)~\emph{Truth vs.\ guidance:} prompt fitness is recomposed from predicted components with the same closed form as the simulator (Eq.~\eqref{eq:fitness}), but archive insertion always uses simulated metrics; DSA-ME's online inner/outer split is closer to the after-generation surrogate than to the before-generation surrogate, yet still does not inject surrogate text into an LLM emitter.
(iv)~\emph{Disclosure:} the offline diagnosis for blocked production H3 is appendix-only (\S\ref{sec:h3-appendix}), not a main-plot branch.
We therefore treat SAIL/DSA-ME as related surrogate-QD lineages rather than drop-in baselines; pyribs CMA-ME/CMA-MAE (\S\ref{sec:pyribs}) are the quantitative QD baselines for this protocol.

\subsection{LLMs in evolutionary and QD search}

LLMs have been used as variation operators, prompt optimizers, and evolutionary engines~\cite{wu2025ecllm,wang2025llmea,vanstein2024llamea,guo2024evoprompt,liu2024llmeo,lange2024llmes}. Program-search lines pair a frozen LLM with an external evaluator to evolve code or rewards (FunSearch; Eureka)~\cite{romera2024funsearch,ma2024eureka}; Promptbreeder evolves task and mutation prompts self-referentially~\cite{fernando2024promptbreeder}; OpenELM surveys open-ended LLM evolution~\cite{lehman2023openelm}. Closest to our QD setting, LLMatic couples LLM code generation with CVT-MAP-Elites for neural architecture search~\cite{nasir2024llmatic}, and in-context QD generators sample diverse solutions from LLM priors~\cite{lim2024llmqd}. Behaviour-space analyses of LLaMEA further compare dual-prompt mutation strategies on anytime search dynamics~\cite{vanstein2025behaviour}. Concurrent LLM+MAP-Elites applications include D{\'e}j{\`a}Q's learnability-guided curation of RL-with-verifiable-rewards datasets~\cite{ropke2026dejaq} and medical decision-pipeline evolution without a surrogate/gating layer~\cite{sviridov2026medical}---both use LLM variation in a QD loop, but neither treat before- vs.\ after-generation surrogates as an experimental factor. Procedural-content pipelines apply evolutionary search and LLMs to level generators and structured genotypes~\cite{togelius2010sbpcg,earle2022nca}; emitters must respect simulators that validate and repair invalid outputs (as in our dungeon domain, \S\ref{sec:results}). Open-ended CA worlds have likewise been illuminated with QD archives~\cite{faldor2024lenia}.

A complementary line treats the \emph{prompt itself} as the search space: Diverse Prompts applies MAP-Elites over prompt phenotypes for BigBench Lite tasks~\cite{santos2025diverseprompts}, evolutionary sampling explores instruction diversity more broadly~\cite{saletta2024promptspace}, and parameter-efficient neuroevolution steers frozen LLMs by evolving prompt embeddings rather than using the LLM as a genotype emitter~\cite{guo2026qdllm}. Those settings illuminate prompts (or embeddings) for a fixed downstream LLM call, whereas we mutate \texttt{WorldSpec} genomes and inject simulator-derived \emph{surrogate scalars} into the mutation prompt. A different surrogate use keeps the LLM on fitness estimation: LLM-assisted evolutionary algorithms (LAEA) and related LLM-driven SAEAs prompt a model to classify or regress candidate quality~\cite{hao2024laea,xie2025llmsae}. We instead pair a genome-aligned ML regressor (trained on simulator logs) with an LLM \emph{emitter} that edits structured genotypes---the LLM is \emph{not} the surrogate.

\subsection{Seeded simulators and uncertain QD}

Prior LLM+QD systems often evaluate on deterministic or batched offline tasks. Our confirmatory evaluations are \emph{deterministically seeded} from the genotype (canonical SHA-256), so re-evaluating the same candidate reproduces the same fitness and BC even when the underlying simulator rules are stochastic. Elites are therefore not lucky-seed artifacts in the UQD sense.

Uncertain QD (UQD) formalizes settings where fitness and BC are distributions and archive policies re-sample to limit lucky outliers~\cite{flageat2023uq,flageat2025extractqd,flageat2025tradeoff}. We use a single seeded pass per insertion and paired experiment seeds with Holm on terminal coverage and mean elite fitness (\S\ref{sec:protocol}). We separate \emph{archive truth} (always simulated) from \emph{emitter guidance} (before- and/or after-generation surrogates). Target selection is orthogonal to those two surrogate roles.

Table~\ref{tab:positioning} summarizes how the present work relates to the closest prior clusters.

\begin{table}[!htbp]
  \centering
  \caption{Positioning vs.\ related QD + surrogate + LLM lines.}
  \label{tab:positioning}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llp{3.2cm}l@{}}
    \toprule
    Line & Surrogate & LLM role & Skip real eval? \\
    \midrule
    Classical SAEA model mgmt.\cite{jin2005fitness} & Approx.\ fitness; individual / generation control & --- & Often (pre-selection) \\
    SAIL / DSA-ME & GP / deep predictor as search surface & --- & Often yes \\
    LAEA / LLM-SAEA & LLM \emph{as} fitness model & Surrogate / ranker & Sometimes \\
    LLMatic & --- & Code/variation emitter & No (NAS training) \\
    Prompt-aug.\ LLM search & Text advice (often none) & Mutator / coder & Usually no \\
    \textbf{This work} & Shared MLP; soft prompt vs.\ hard gate & Structured mutator (not the surrogate) & After-gen.\ gate only$^\dagger$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}
\noindent\footnotesize$^\dagger$Skip is optional: prompt-only arms evaluate every proposal; only the after-generation gate may skip simulation (the confirmatory hard-gate claim is the matched genetic filter contrast).\normalsize

\noindent\textit{Key difference from SAIL:} we do not replace MAP-Elites search with a GP acquisition illuminator; we ask \emph{where} a fixed MLP checkpoint attaches in an LLM-guided propose$\rightarrow$evaluate loop, and we gate with a fitness threshold rather than UCB over a GP surface (\S\ref{sec:acquisition}). The after-generation surrogate is intentionally SAIL-\emph{kin} and classical individual-based pre-selection (hard evaluation gate); the before-generation surrogate is the soft, non-controlling channel (prompt scalars that never skip simulation) that makes the before/after comparison non-redundant with SAIL-style illumination alone.
\textit{Key difference from DSA-ME:} predictors are an offline shared checkpoint feeding an optional before-generation surrogate and after-generation skips, not an online inner-loop illuminator with outer game-engine validation.
\textit{Key difference from LAEA:} the LLM never estimates fitness---the MLP does; the LLM only proposes structured genomes.
\textit{Key difference from classical SAEA generation-based control:} we do not alternate approximate vs.\ exact \emph{generations}; both H1 and H2 operate per candidate slot inside a fixed MAP-Elites batch schedule.

\section{Method}
\label{sec:method}

Everything below implements one contrast: before- vs.\ after-generation attachment of a shared surrogate in a MAP-Elites propose$\rightarrow$evaluate loop (Figure~\ref{fig:pipeline}). The LLM is an optional emitter; the evaluator is a measurement substrate only (\S\ref{sec:substrate}). Archive fitness always comes from the simulator---the surrogate never replaces ground truth for insertion.

\begin{figure}[!htbp]
  \centering
  \pagefitfig{fig01_pipeline.pdf}
  \caption{Architectural attach-role map for LLM-guided MAP-Elites: \emph{before-generation surrogate} (H1) vs.\ \emph{after-generation surrogate} (H2). Stacking both under LLM is H3.}
  \label{fig:pipeline}
\end{figure}

\subsection{Attachment points and contrast labels}
\label{sec:terminology}

A MAP-Elites iteration is select $\rightarrow$ \textbf{generate} $\rightarrow$ (optionally) gate $\rightarrow$ \textbf{evaluate} $\rightarrow$ archive update. As in Figure~\ref{fig:pipeline}:

\begin{itemize}
  \item \textbf{Before-generation surrogate} --- predictions enter the emitter and try to change \emph{what} is proposed (never skips simulation).
  \item \textbf{After-generation surrogate} --- predictions gate the candidate and change \emph{whether} it spends ground truth (SAIL-\emph{kin} hard gate).
\end{itemize}

We use those two names throughout. Synonyms appear only here: before-generation $\equiv$ prompt hints / soft intervention; after-generation $\equiv$ acquisition / candidate filtering / hard intervention; attachment point $\equiv$ intervention point / surrogate attachment policy. Table~\ref{tab:h-status} is the single status board for labels \textbf{H1}--\textbf{H5}; later prose uses those status words only. Prompt placeholders and thresholds are mechanisms \emph{inside} a role. Surrogate-informed parent selection is out of scope; archive \texttt{target\_selection} remains ordinary MAP-Elites policy.

\begin{table}[!htbp]
  \centering
  \caption{H1--H5 status board (single source of truth). Status vocabulary: \textbf{TOST-accept} (flat within band); \textbf{pass} (confirmatory family / eval-axis claim); \textbf{fail} (confirmatory family does not pass); \textbf{blocked} (pre-specified validity gate fails; levels descriptive only); \textbf{NI-pass-not-superiority} (non-inferiority margins pass; mean $\Delta$cov still $\le 0$); \textbf{partial} (subset of Holm tests reject). Bundled stub$\rightarrow$hints is \emph{not} H1 (appendix Table~\ref{tab:families}).}
  \label{tab:role-exp}
  \label{tab:h-status}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llp{3.6cm}l@{}}
    \toprule
    Label & Contrast & Claim tested & Status \\
    \midrule
    H1 & \texttt{stub\_uniform} vs.\ \texttt{hints} & soft before-gen.\ scalars & \textbf{TOST-accept} (flat; $\pm 2$~pp) \\
    H2 & \texttt{genetic\_me\_uniform} vs.\ \texttt{genetic\_me\_filter} & hard after-gen.\ eval efficiency & \textbf{pass} (eval-indexed; terminal $-0.83$~pp / slower wall on this evaluator) \\
    H3-prod & \texttt{hints} vs.\ production \texttt{filter} & LLM$+$filter stack superiority & \textbf{blocked} (compose-gate $d{=}0.528$) \\
    H3-gray & \texttt{hints} vs.\ gray-zone filter & LLM$+$filter NI margins & \textbf{NI-pass-not-superiority} ($m{=}3$; mean $\Delta$cov $-0.60$~pp) \\
    H4 & hints $>$ cma\_me \textbf{and} cma\_mae & LLM stack beats continuous-relax.\ pyribs & \textbf{fail} (CMA-ME leads ${\sim}13$~pp) \\
    H5 & maze AUC family (main) & after-gen.\ / LLM transfer & \textbf{partial} (genetic filter AUC yes; LLM arms no; dungeon appendix) \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

Shared-checkpoint hold-out is a validity gate before H1--H3, not a separate H-label.

\subsection{MAP-Elites illuminator loop}

Each run executes \texttt{iterations} $\times$ \texttt{batch\_size} candidate slots in fixed order. Production schedulers use $650 \times 50 = 32{,}500$ simulator calls per seed. For slot~$k$ the loop:
\begin{enumerate}
  \item selects a target niche (behaviour-cell centre) according to \texttt{target\_selection};
  \item calls the slot emitter (\texttt{random}, \texttt{genetic}, or \texttt{llm}) to propose a \texttt{WorldSpec};
  \item optionally applies surrogate acquisition (\S\ref{sec:acquisition});
  \item runs \texttt{evaluate\_candidate} $\rightarrow$ \texttt{run\_world} (always for accepted slots in MVP hint mode);
  \item inserts into the archive if fitness strictly improves the cell incumbent;
  \item appends training data to the surrogate buffer when \texttt{surrogate.enabled}.
\end{enumerate}
LLM slots log \texttt{llm\_fallback} when parsing fails and a genetic repair path is used. Parallel evaluation uses a process pool sized to the batch; LLM emit uses up to four concurrent HTTP workers per iteration. Outputs: \texttt{map\_elites\_archive.jsonl}, optional \texttt{archive\_trace.jsonl}, and \texttt{nightly\_run\_summary.json}.

\textbf{Target selection.} \texttt{min\_fitness\_frontier} picks the occupied cell with lowest elite fitness (bundled \texttt{stub}). \texttt{uniform\_frontier} samples uniformly among occupied cells (\texttt{hints}, filter arms, matched H1/H2). These are archive policies, not surrogate roles: they do not use the surrogate to select parents. Scientific H1 and H2 hold this policy fixed (\S\ref{sec:decomposition}, \S\ref{sec:h2}). The historical stub$\rightarrow$hints gap does not. Surrogate-assisted parent selection is out of scope.

\subsection{Surrogate model}
\label{sec:surrogate-method}

The surrogate maps a canonical \texttt{WorldSpec} to predicted MAP-Elites outcomes without a simulator call (Figure~\ref{fig:surrogate}). Feature schema~2.1 encodes 24 genome-aligned dimensions (discrete rule bits and continuous scalars; \S\ref{sec:substrate}). A multi-head regressor (\textbf{Strategy~A}) predicts seven component targets---stability, diversity, oscillation, two topology proxies, final density, and early-extinction probability---trained from JSONL buffer rows appended after each real evaluation when the surrogate is enabled.

Production checkpoint \texttt{nightly\_v3\_mc\_d005.pkl} is a four-member MLP ensemble with MC-dropout uncertainty ($N=35{,}114$ training rows; hold-out at runtime-aligned compose gate~$0.95$: $R^2_{\text{fitness}}=0.94$ [95\% CI $0.94{,}0.95$], MAE$_{\text{fitness}}=0.029$, NMAE$=0.112$; appendix~\S\ref{sec:checkpoint-appendix}). Runtime fitness is recomposed via \texttt{compute\_fitness\_from\_prediction} using Eq.~\eqref{eq:fitness} (same weights as the simulator). \texttt{get\_surrogate()} falls back to constant \texttt{StubSurrogate} values if the checkpoint is missing, dimension-mismatched, or fails the \texttt{hints\_ok} gate ($R^2 \ge 0.30$, MAE $< 0.085$).

\textbf{Before-generation surrogate (H1).} Before each LLM call, \texttt{SurrogateFacade.predict(parent)} runs on the \emph{parent} elite's spec. Placeholders \texttt{\{surrogate\_mean\}} and \texttt{\{surrogate\_uncertainty\}} in the user prompt receive \texttt{prediction.fitness} and calibrated ensemble spread. When \texttt{surrogate.enabled=false}, fixed YAML stubs (\texttt{stub\_mean}, \texttt{stub\_uncertainty}) are substituted without calling \texttt{predict}. One LLM HTTP request per LLM slot regardless of surrogate mode.

\subsection{LLM emitter}

The \texttt{LlmEmitter} sends the parent \texttt{WorldSpec} JSON and niche centre to a chat-completions API; the model returns a JSON patch applied via \texttt{world\_spec\_from\_llm}. Stack~v2 uses temperature~0.2, \texttt{max\_tokens=350}, and \texttt{llm\_parallel\_workers=4}. Primary LLM runs use Alibaba \texttt{qwen-turbo} (DashScope); multi-provider replications swap provider YAML while keeping prompts and schedulers fixed (\S\ref{sec:results}). Model IDs are window-pinned in run summaries (\texttt{llm\_spec\_hash}); floating endpoint aliases are disclosed per provider.

Accepted LLM elites typically differ from parents by compact named-field rule/scalar edits rather than bit noise (edit-anatomy case study, appendix~\S\ref{sec:edit-anatomy}).

\subsection{After-generation surrogate (H2)}
\label{sec:acquisition}

Optional \texttt{acquisition.mode=filter} evaluates \texttt{surrogate.predict} \emph{before} \texttt{evaluate\_candidate}. A \texttt{threshold\_gate} skips simulation when predicted fitness falls below \texttt{min\_predicted\_fitness=0.45} (primary grid) subject to uncertainty calibration. Skipped slots are logged to \texttt{surrogate\_archive.jsonl} but do not update the MAP-Elites archive. \texttt{shadow} mode logs counterfactual skips while still evaluating. We use an MLP ensemble with a fitness threshold---not SAIL-style GP+UCB---as the production after-generation mechanism; offline MLP-vs-GP/UCB comparisons are appendix~\S\ref{sec:surrogate-ablation-appendix}. Primary H2 evidence is the matched genetic filter contrast (\S\ref{sec:h2}); LLM$+$filter stacking is H3-prod \textbf{blocked} / H3-gray \textbf{NI-pass-not-superiority} (Table~\ref{tab:h-status}; \S\ref{sec:h3}).

\subsection{pyribs CMA-ME / CMA-MAE baselines}
\label{sec:pyribs}

Gradient QD baselines run through \texttt{run\_pyribs\_baseline.py} (\texttt{ribs==0.11.0}), not as native \texttt{EmitterKind} types. The adapter maps the same 21-D genetic genome to \texttt{WorldSpec}, evaluates with the same simulator, and writes comparable JSONL archives. Each algorithm uses five homogeneous \texttt{EvolutionStrategyEmitter} instances (batch~50 $\Rightarrow$ ask size~250), $\sigma_0=0.2$, and 32{,}500 evaluations. CMA-ME uses learning rate~1.0 with 2-improvement ranking; CMA-MAE uses learning rate~0.01 with a separate result archive. This is a strong but not maximal pyribs configuration (no mixed ImprovementEmitter cocktail). Encoding asymmetries are disclosed below.

\textbf{Encoding (continuous relaxation).}
CMA proposes continuous $\theta\in\mathbb{R}^{21}$; \texttt{decode\_genome} then \emph{rints} the 18 rule bits and clips the three scalars. Genetic / LLM emitters edit discrete rule tables directly. The comparison is therefore not bit-identical: CMA searches a continuous relaxation where covariance adaptation can exploit structure that bit-flip genetics never sees. We treat pyribs arms as an upper-bound-style ceiling for continuous-friendly representations. Decode / discrete-search controls (Bernoulli, native bit-flip, pbCMA) are appendix~\S\ref{sec:encoding-appendix}.

\textbf{Warm-start (symmetric).} All confirmatory primary-grid arms---\texttt{vanilla}, genetic ME variants, \texttt{stub}/\texttt{hints}/\texttt{filter}, and pyribs CMA-ME/MAE---load the \emph{same} nightly grid baseline JSONL ($\sim$2{,}614 elites) via \texttt{--load-archive} before the 32{,}500-evaluation budget. Terminal coverage is reported after warm-start merge/collapse. The pyribs contrast is matched on initialization as well as evaluation count; asymmetries arise from emitter pipelines and continuous-relaxation encoding, not from unequal archive seeds.

\begin{figure}[!htbp]
  \centering
  \pagefitfig{fig03_surrogate_flow.pdf}
  \caption{Shared surrogate (simplified): genome features feed one MLP; outputs fork to H1 prompt scalars and H2 threshold gate. Archive updates use ground-truth simulation only (training-buffer loop omitted; see repository docs).}
  \label{fig:surrogate}
\end{figure}

\subsection{Measurement substrate}
\label{sec:substrate}

Primary contrasts use a controlled black-box evaluator---not a domain contribution. Each candidate is a structured \texttt{WorldSpec}: birth/survival neighbour-count rule tables (18 bits) plus three continuous scalars (\texttt{noise}, \texttt{resource\_regen}, \texttt{predation}). The simulator runs a seeded $50\times50$ toroidal cellular-automaton world for $T_{\mathrm{sim}}{=}200$ steps (early extinction $\Rightarrow$ fitness~0). Behaviour characteristics are stability and diversity in $[0,1]$, so the primary archive is a $50\times50$ niche grid over that plane; insertion fitness is a fixed weighted recompose of diversity, survival, oscillation, and topology proxies (Eq.~\eqref{eq:fitness} in appendix~\S\ref{sec:substrate-appendix}). Genetic emitters flip bits / nudge scalars; LLM emitters edit the same named fields as JSON. The evaluator is cheap relative to CFD-style SAIL domains, so H2 claims evaluation-indexed efficiency and reports wall time separately (\S\ref{sec:cost}). Structured named fields make both soft before-generation and hard after-generation interventions testable---the reason for a synthetic CA primary testbed rather than a pure $\mathbb{R}^d$ literature toy as the sole domain. Table~\ref{tab:domain} lists the knobs; example elites and fitness algebra remain in the appendix. Coverage is occupied-cell percentage after warm-start merge; mean best fitness averages elites over filled cells; QD-score (Eq.~\eqref{eq:qd}) is descriptive only. CVT geometry is appendix sensitivity~\cite{vassiliades2018cvt}. Complementary literature Sphere H2 transfer (threshold gate only; no LLM) is appendix~\S\ref{sec:sphere-h2-appendix}.

\begin{table}[!htbp]
  \centering
  \caption{Primary evaluator knobs (grid tier).}
  \label{tab:domain}
  \fitcenter{%
  \begin{tabular}{@{}llp{6.2cm}@{}}
    \toprule
    Knob & Setting & Role \\
    \midrule
    Field size / horizon & $50 \times 50$, $T_{\mathrm{sim}}{=}200$ steps & Fixed simulator budget per eval \\
    Early extinction & step $< 200$ & Zero fitness if population dies out \\
    BC axes & stability, diversity $\in [0,1]$ & 50$\times$50 niche grid \\
    Genome & 18 rule bits + 3 scalars & Structured discrete/continuous genotype \\
    Batch emitters & 20R + 20G + 10L & Heterogeneous MAP-Elites pool \\
    Evaluations / run & $650 \times 50 = 32{,}500$ & Matched across primary-grid conditions \\
    \bottomrule
  \end{tabular}%
  }
\end{table}


\section{Experimental protocol}
\label{sec:protocol}

Confirmatory contrast sets are frozen \emph{per family} in dated repository protocol files relative to that family's analysis window (primary-grid H1/H2/H4; maze H5; appendix dungeon)---not as a single external OSF/Zenodo pre-registration of the whole paper. Later rows add packages, repair paths, or reporting statuses; they do not silently rewrite already-frozen Holm families (appendix~\S\ref{sec:amendments-appendix}, Table~\ref{tab:timeline}). Descriptive and pilot arms are not pooled into confirmatory Holm tests.

\subsection{Design principles}

\textbf{Paired seeds.} All primary-grid contrasts use the same integer seeds $0,\ldots,9$ across conditions within a tier. The maze factorial (and appendix dungeon) use the same seed indices with independent domain RNG.

\textbf{Primary archive and warm-start.} Confirmatory primary-grid endpoints use the \textbf{50$\times$50 grid}. Every primary-grid arm in the batch driver warm-starts from the same baseline JSONL before the matched evaluation budget (\S\ref{sec:pyribs}).

\textbf{Multiple comparisons.} Each confirmatory contrast set uses Holm step-down on one-sided Wilcoxon tests at $\alpha=0.05$ (within set). A set passes only if every test in that set rejects (and local effect-size gates where the frozen protocol requires them). Matched H1 TOST sits outside primary-grid confirmatory Holm sets; H3-gray is the separate NI family ($m{=}3$; Table~\ref{tab:h-status}). A paper-wide Holm check over the pooled primary-grid $p$-vector agrees on every claim-driving reject/non-reject (appendix~\S\ref{sec:stats-appendix}).

\textbf{Power and non-rejection.} Formal paired-$t$ MDE / TOST power is reported for matched-policy H1 ($|\Delta\mathrm{cov}|\le 2$~pp; Table~\ref{tab:decomposition}). Elsewhere, a Holm non-reject is failure to reject. Retrospective MDEs from observed paired SDs appear in appendix~\S\ref{sec:stats-appendix} and do not change locked pass/fail rules. H4 is \textbf{fail}; H3-prod \textbf{blocked}; H3-gray \textbf{NI-pass-not-superiority} (Table~\ref{tab:h-status}).

\textbf{Bundled stub vs.\ hints.} The locked \texttt{stub} vs.\ \texttt{hints} contrast differs in surrogate use and in \texttt{target\_selection} (\S\ref{sec:method}). Scientific H1 holds target selection fixed (\S\ref{sec:decomposition}).

\subsection{Experimental conditions}

Table~\ref{tab:conditions} lists arms referenced in the main text. Each primary-grid run executes 32{,}500 simulator evaluations unless noted. Emitter mix for mixed arms is 20 random + 20 genetic + 10 LLM (Table~\ref{tab:domain}).

\begin{table}[!htbp]
  \centering
  \caption{Main experimental conditions (primary grid tier unless noted).}
  \label{tab:conditions}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llllp{3.8cm}@{}}
    \toprule
    Label & Emitters & Surrogate & LLM & Notes \\
    \midrule
    \texttt{vanilla} & 50$\times$ random & off & off & Random floor \\
    \texttt{genetic\_me} & 20R+30G & off & off & Non-LLM ME; weak policy \\
    \texttt{genetic\_me\_uniform} & 20R+30G & off & off & H2 control \\
    \texttt{genetic\_me\_filter} & 20R+30G & filter & off & H2 treatment \\
    \texttt{stub} & mixed & stub constants & on & Bundled control; weak policy \\
    \texttt{hints} & mixed & live hints & on & Bundled / H1 treatment \\
    \texttt{filter} & mixed & filter & on & H3-prod (blocked; levels only) \\
    \texttt{cma\_me} / \texttt{cma\_mae} & 5$\times$ pyribs ES & off & off & Continuous-relaxation reference \\
    \texttt{genetic} (maze) & 20R+30G & off & off & Main-text H5; 900-cell grid \\
    \texttt{llm\_hints} (maze) & 20R+30L & maze surr. & on & 32.5k proposals/arm ($n{=}10$) \\
    \texttt{genetic}/\texttt{llm\_*} (dungeon) & as maze & dungeon surr. & mixed & Appendix~\S\ref{sec:dungeon-appendix} \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\subsection{Confirmatory contrast sets (summary)}

H1--H5 statuses are Table~\ref{tab:h-status} only. Bundled stub$\rightarrow$hints / hints$\rightarrow$vanilla stack rows (appendix Table~\ref{tab:families}) are confounded floor checks, not H1. Software pins: Table~\ref{tab:pins}.

\subsection{Reproducibility pins}
\label{sec:pins}

Table~\ref{tab:pins} lists frozen protocol documents, software pins, and window-pinned LLM endpoints used in confirmatory and multi-provider tiers. Primary bundled primary-grid runs predate per-run \texttt{llm\_spec\_hash} logging; later multi-provider and ablation summaries record the hash explicitly.

\begin{table}[!htbp]
  \centering
  \caption{Reproducibility pins (protocol files, software, and window-pinned LLM endpoints).}
  \label{tab:pins}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llp{6.4cm}@{}}
    \toprule
    Item & Pin & Notes \\
    \midrule
    Protocol (primary grid) & Q1 v2 + v3 & §§1--4 frozen 2026-07-12 (v3); v2 grid matrix \\
    Protocol dungeon & Q1 v4 & Frozen 2026-07-17; appendix H5 @ 5k proposals \\
    Maze batch (main-text H5) & H5 maze $n{=}10$ & five-arm @ 32.5k; see Data availability \\
    Code revision (submission) & tag~\texttt{\SubmissionGitTag} / \texttt{\SubmissionGitShort} & Fill SHA at upload; see Data availability \\
    Seeds (primary grid) & $0,\ldots,9$ & Paired across conditions within tier \\
    Eval budget (primary grid) & 32{,}500 & $650\times50$ simulator calls \\
    Archive & $50\times50$ grid & Confirmatory; CVT = appendix only \\
    Surrogate checkpoint & \texttt{nightly\_v3\_mc\_d005.pkl} & Schema~2.1, 24-D; MLP ens.\ 4; MC-drop $p{=}0.05$ \\
    Hold-out (shared-checkpoint validity) & $R^2{=}0.94$ $[0.94,0.95]$, MAE$_{\text{fit}}{=}0.029$, NMAE$=0.112$ & gate~$0.95$ aligned with runtime; target std$\times 1.92$ vs gate~$0.5$ ($R^2{=}0.76$, MAE$=0.007$, NMAE$=0.052$); not a model upgrade \\
    LLM stack & v2 & $T{=}0.2$, \texttt{max\_tokens}{=}350, 4 workers \\
    Prompt (grid H1) & \texttt{2fc7bdc1:e2afd1e9} & System{:}user composite hash \\
    H1 model & \texttt{qwen-turbo} (DashScope) & Window 2026-07-02\ldots2026-07-11; floating alias \\
    Multi-prov.\ DeepSeek & \texttt{deepseek-v4-pro} & \texttt{llm\_spec\_hash=fd5f0d34f97f9df7}; seeds 0--9 \\
    Multi-prov.\ OpenAI & \texttt{gpt-4o-mini} & \texttt{llm\_spec\_hash=b678837d32da4f49}; seeds 0--9 \\
    pyribs & \texttt{ribs==0.11.0} & CMA-ME / CMA-MAE adapter \\
    Dungeon surrogate & \texttt{checkpoint\_benchmark\_v2.pkl} & Separate from primary-grid nightly checkpoint \\
    Dungeon prompt & \texttt{4d25cb08b88ea3f4} & Dungeon tile-LLM user/system pin \\
    Maze filter gate & $\tau{=}0.685$, unc.\ cap $0.0141$ & Calibrated skip $\sim$27\% \\
    Maze LLM prompt & \texttt{c5def71acc84f0db} & Maze emitter v2 (retry + coerce) \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\subsection{Endpoints and statistical reporting}
\label{sec:stats}

\textbf{Primary endpoints:} terminal coverage (\%) and mean best elite fitness on the collapsed archive. Confirmatory inference uses paired seeds ($n{=}10$ unless noted), one-sided Wilcoxon signed-rank tests, and Holm within each pre-specified contrast set (\S\ref{sec:protocol}). Tables may also report Kerby's paired proportion of favorable pairs~\cite{kerby2014simple} (ties as half), abbreviated here as $\mathrm{PPS}$ only for column headers (Eq.~\eqref{eq:pps}; appendix~\S\ref{sec:stats-appendix})---the same estimand, not a new metric; distinct from independent-sample $A_{12}$~\cite{vargha2000new}, whose magnitude conventions (e.g.\ 0.56/0.64/0.71) do not transfer. Matched-policy H1 uses a post-hoc TOST at $|\Delta\mathrm{cov}|\le 2$~pp (\S\ref{sec:decomposition}). Pilot arms report mean $\Delta$, sign counts, and bootstrap CIs. Surrogate hold-out $R^2$/MAE/NMAE are validity checks, not endpoints (\S\ref{sec:checkpoint}).

\subsection{Maze factorial (main-text H5; $n{=}10$)}

Main-text cross-domain H5 is a maze factorial at the primary-grid proposal budget (\textbf{32{,}500} proposals/arm; 900-cell behaviour grid; five arms; separate maze surrogate and filter calibration $\tau{=}0.685$). Endpoints: coverage / QD-score AUC at matched real evaluations (Holm $m{=}8$). All five arms complete on seeds $0$--$9$ ($n{=}10$). Quality gates pass (pooled LLM fallback $0.009\%$; genetic\_filter skip $27.3 \pm 0.6\%$). A parallel dungeon factorial (5k proposals; locked v4) is appendix~\S\ref{sec:dungeon-appendix} / journal-extension material---not required for the main H5 readout.

\section{Results}
\label{sec:results}

Every contrast below probes the same intervention-point model. Matched H1/H2 isolate soft before-generation vs.\ hard after-generation intervention; H4 and maze H5 bound that answer (H3 / multi-provider detail: appendix). All confirmatory tests use paired seeds ($n=10$ unless noted), one-sided Wilcoxon signed-rank tests, and Holm within each confirmatory contrast set ($\alpha=0.05$; \S\ref{sec:stats}). Primary endpoints are archive coverage (\%) and mean best elite fitness. Shared-checkpoint hold-out is a one-line validity gate ($R^2{=}0.94$, NMAE$=0.112$ at runtime compose~$0.95$; passes \texttt{hints\_ok}); gate-alignment algebra and MLP-vs-GP/UCB ablations are appendix~\S\ref{sec:checkpoint-appendix} / \S\ref{sec:surrogate-ablation-appendix}.

\subsection{Bundled stack contrast (motivation)}

We first report the locked contrast that motivated matched H1: \texttt{stub} vs.\ \texttt{hints} on the primary grid ($n=10$, 32{,}500 evaluations). Figure~\ref{fig:rq1} summarizes levels and paired deltas. Scientific H1 asks whether that gap is caused by hint text (\S\ref{sec:decomposition}).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{fig04_rq1_rq0.pdf}
  \caption{Bundled stub vs hints on the primary grid ($n=10$ paired seeds): levels and per-seed paired deltas (stack contrast; see Table~\ref{tab:rq1}).}
  \label{fig:rq1}
\end{figure}

\begin{table}[!htbp]
  \centering
  \caption{Bundled stack: paired hints $-$ stub (grid, $n=10$). $\mathrm{PPS}$ = Kerby paired proportion of favorable pairs (Eq.~\eqref{eq:pps}). Holm passes as a stack outcome; matched H1 is Table~\ref{tab:decomposition}.}
  \label{tab:rq1}
  \fitcenter{%
  \begin{tabular}{@{}lcccccp{2.4cm}@{}}
    \toprule
    Metric & Mean $\Delta$ & Median $\Delta$ & Sign & $\mathrm{PPS}$ & Wilcoxon $p$ & Causal for H1? \\
    \midrule
    Coverage (pp) & $+15.09 \pm 2.42$ & $+15.20$ & 10/10 & 1.00 & $9.8\times10^{-4}$ & \textbf{No} (confounded) \\
    Mean best fitness & $+0.047 \pm 0.010$ & $+0.046$ & 10/10 & 1.00 & $9.8\times10^{-4}$ & \textbf{No} (confounded) \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

Mean terminal levels: stub $45.32 \pm 2.44$\% coverage, fitness $0.452 \pm 0.041$; hints $60.41 \pm 0.82$\%, fitness $0.499 \pm 0.008$. Bootstrap 95\% CI on median $\Delta$: coverage $[+13.2, +17.1]$~pp; fitness $[+0.038, +0.054]$. Holm $m=2$ rejects both endpoints. Per-seed deltas: Table~\ref{tab:rq1-seeds} (appendix).

\subsection{H1 --- Before-generation surrogate}
\label{sec:decomposition}

\textbf{H1 answer.} At matched \texttt{uniform\_frontier}, live prompt scalars vs.\ stub constants are flat (Table~\ref{tab:decomposition}; multi-provider replication Table~\ref{tab:h1-matched-providers}). On \texttt{qwen-turbo} ($n{=}10$): mean coverage $60.15 \pm 0.73\%$ vs.\ $60.41 \pm 0.82\%$ ($\Delta = +0.26$~pp; SD~$1.19$~pp; Wilcoxon $p{=}0.41$; bootstrap 95\% CI $[-0.43, +0.98]$~pp). Post-hoc TOST at $|\Delta\mathrm{cov}| \le 2$~pp accepts ($p_{\mathrm{TOST}}{=}6.4\times10^{-4}$; 90\% CI $\subset [-2,2]$). MDE ${\approx}1.2$~pp (Eq.~\eqref{eq:mde}). Changing target selection alone recovers ${\approx}{+}14.8$~pp of the historical $+15$~pp gap (\texttt{stub\_uniform} $-$ \texttt{stub}). On \texttt{gpt-4o-mini} and DeepSeek V4 Pro ($n{=}10$ each), matched $\Delta$cov is $+0.12 \pm 1.02$~pp and $+1.02 \pm 3.51$~pp; both pass the same $\pm 2$~pp TOST band (Wilcoxon non-significant). Enriched / weak-model pilots ($n{\le}3$; Table~\ref{tab:hint-pilots}) stay within $|\Delta\text{cov}| < 2$~pp. Multi-provider Table~\ref{tab:g1} repeats the bundled stack contrast. After-generation results follow (\S\ref{sec:h2}).

\begin{table}[!htbp]
  \centering
  \caption{Matched-policy hint channel (H1): \texttt{hints} $-$ \texttt{stub\_uniform} on seeds~0--9 ($n{=}10$). Post-hoc TOST/MDE (outside confirmatory Holm sets). Pilots: Table~\ref{tab:hint-pilots}.}
  \label{tab:decomposition}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lp{7.2cm}@{}}
    \toprule
    Quantity & Value \\
    \midrule
    Mean coverage (stub\_uniform / hints) & $60.15 \pm 0.73\%$ / $60.41 \pm 0.82\%$ \\
    Mean fitness (stub\_uniform / hints) & $0.496 \pm 0.005$ / $0.499 \pm 0.005$ \\
    Mean $\Delta$cov (hints $-$ stub\_uniform) & $+0.26$~pp (SD~$1.19$~pp) \\
    Bootstrap 95\% CI (mean $\Delta$cov) & $[-0.43, +0.98]$~pp \\
    Wilcoxon (two-sided) & $p{=}0.41$ \\
    TOST $|\Delta\mathrm{cov}|\le 2$~pp (post-hoc) & accept ($p_{\mathrm{TOST}}{=}6.4\times10^{-4}$; 90\% CI $\subset[-2,2]$) \\
    MDE (Eq.~\eqref{eq:mde}) & ${\approx}1.2$~pp (power ${\approx}0.66$ @ 1~pp; ${\approx}0.997$ @ 2~pp) \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{Pilot runs ($n{=}1$ / $n{\le}3$). Enriched / weak-model hint pilots stopped by a pre-set $|\Delta\mathrm{cov}|<2$~pp gate. Not co-tabulated with the matched H1 table (Table~\ref{tab:decomposition}).}
  \label{tab:hint-pilots}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llrp{3.2cm}@{}}
    \toprule
    Arm & Seeds & $\Delta$cov vs.\ \texttt{hints} & Reading \\
    \midrule
    \texttt{hints\_rich} & 0 ($n{=}1$) & $+0.40$~pp & illustrative; underpowered \\
    \texttt{hints\_parent} & 0 ($n{=}1$) & $+0.20$~pp & illustrative; underpowered \\
    \texttt{hints\_direction} & 0 ($n{=}1$) & $-0.68$~pp & illustrative; underpowered \\
    \texttt{hints\_weak} (omni-7b) & 0--2 ($n{=}3$) & $-0.08$~pp vs.\ stub\_uniform & illustrative; underpowered \\
    \bottomrule
  \end{tabular}%
  }
  \vspace{0.35em}
  \noindent\small\textit{Note:} $n{=}1$ / $n{\le}3$ pilots; no inferential statistics computed (no $p$-values, no Holm). Matched H1 at $n{=}10$ on additional providers: Table~\ref{tab:h1-matched-providers}.
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{Matched-policy H1 on additional LLM providers ($n{=}10$; hints $-$ stub\_uniform @ \texttt{uniform\_frontier}; post-hoc TOST outside confirmatory Holm). Primary \texttt{qwen-turbo} row: Table~\ref{tab:decomposition}.}
  \label{tab:h1-matched-providers}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrrrrl@{}}
    \toprule
    Provider & Mean $\Delta$cov & SD & Wilcoxon & TOST $\pm 2$~pp & Reading \\
    \midrule
    gpt-4o-mini & $+0.12$~pp & 1.02 & $p{=}0.94$ & accept & flat \\
    DeepSeek V4 Pro & $+1.02$~pp & 3.51 & $p{=}0.71$ & accept & flat \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\subsection{Non-LLM MAP-Elites baselines (floor + genetic ME)}

\textbf{Bundled floor (passes Holm).} Pure random MAP-Elites (\texttt{vanilla}, 50$\times$ random emitters) reaches $42.36 \pm 0.29$\% coverage and fitness $0.373 \pm 0.005$. Paired hints $-$ vanilla: $\Delta\text{cov} = +18.05 \pm 0.79$~pp (10/10), $\Delta\text{fit} = +0.166 \pm 0.011$ (10/10); Wilcoxon $p = 9.8\times10^{-4}$ for both; Holm $m=2$ rejects; $\mathrm{PPS}{=}1.00$ on both metrics.

Descriptive stub $-$ vanilla: $+2.96$~pp coverage.

\textbf{Genetic MAP-Elites.} \texttt{genetic\_me} (20R+30G, no LLM) achieves $45.93 \pm 1.50$\%---near historical \texttt{stub} ($45.32 \pm 2.44$\%). Matched-policy \texttt{genetic\_me\_uniform} jumps to $59.04 \pm 0.76$\% and is the fair no-LLM reference for H2.

\textbf{Performance ladder.} Figure~\ref{fig:ladder} shows mean terminal coverage with 95\% CI ($n{=}10$). Ordering: vanilla $<$ stub $\approx$ genetic\_me $\ll$ genetic\_me\_uniform $\approx$ genetic\_me\_filter $\approx$ LLM+\texttt{filter} $\lesssim$ hints $\ll$ cma\_me. The mid-band ($\sim$58--60\%) is where H2 and the LLM stack meet. H2's claim is coverage \emph{per simulation}, not a higher terminal bar than \texttt{genetic\_me\_uniform} (\S\ref{sec:h2}). Figure~\ref{fig:anytime-ladder} adds evaluation-indexed curves for vanilla / hints / cma\_me ($n{=}5$, median + IQR): CMA-ME leads from early budget (median $\sim$61\% @ 10k vs.\ hints $\sim$47\%).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{fig05_ladder.pdf}
  \caption{Mean archive coverage with 95\% CI on the mean for \emph{every} bar ($n{=}10$ paired seeds; normal approx.\ $\bar x \pm 1.96\,s/\sqrt{n}$). Historical \texttt{stub}/\texttt{genetic\_me} use \texttt{min\_fitness\_frontier}; mid-band arms share \texttt{uniform\_frontier}. \texttt{genetic\_me\_filter} and LLM+\texttt{filter} are after-generation (H2) arms.}
  \label{fig:ladder}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{fig08_anytime_ladder.pdf}
  \caption{Anytime performance ladder: coverage vs.\ simulator evaluations for \texttt{vanilla}, \texttt{hints}, and \texttt{cma\_me} (median + IQR, $n{=}5$ seeds; selective re-runs with \texttt{archive\_trace.jsonl}). CMA-ME separates early ($\sim$+$14$~pp vs.\ hints @ 10k evals); hints and vanilla converge to their terminal bands by 32{,}500 evals.}
  \label{fig:anytime-ladder}
\end{figure}

\subsection{After-generation surrogate (H2)}
\label{sec:h2}

H2 isolates after-generation attachment on matched no-LLM arms: \texttt{genetic\_me\_uniform} vs.\ \texttt{genetic\_me\_filter} ($n=10$; Figure~\ref{fig:acquisition}).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{fig06_acquisition_anytime.pdf}
  \caption{\textbf{Memory figure (eval-efficiency).} Coverage vs.\ simulator evaluations for \texttt{genetic\_me\_uniform} vs.\ \texttt{genetic\_me\_filter} (median + IQR, $n=10$).}
  \label{fig:acquisition}
\end{figure}

\textbf{Evaluation-indexed} coverage is the primary H2 axis (Figure~\ref{fig:acquisition}). At equal evaluation budgets, filter leads by $+3.65$~pp at 20{,}000 evaluations (10/10 seeds) and reaches 50\%/55\% coverage with 28\%/29\% fewer evaluations. At fixed 650 iterations, filter uses 33.5\% fewer simulator calls, finishes $-0.83$~pp below uniform ($58.21 \pm 1.03\%$ vs.\ $59.04 \pm 0.76\%$), and is slower in wall time ($\sim$20 vs.\ $\sim$7~min). The gain is sample efficiency under a matched proposal schedule.

\textbf{Why MLP can cost more than skipped sims.} Acquisition runs \texttt{predict\_batch} on every proposal, including those later skipped, using a four-member MLP ensemble with MC-dropout ($p{=}0.05$) before the cheap simulator roll-out. Simulator evaluations are process-pooled; surrogate inference is a per-candidate Python path. When $t_{\mathrm{sim}}$ is milliseconds, that path dominates wall. Skipping 33.5\% of sims therefore removes little wall while adding full-batch MLP work.

\textbf{Wall-time break-even (descriptive).} Write wall cost per proposal batch as $(1-s)\,t_{\mathrm{sim}}+t_{\mathrm{MLP}}$ with skip rate $s$. Filter wins on wall only if $t_{\mathrm{MLP}}<s\,t_{\mathrm{sim}}$. On the primary grid ($s{\approx}0.335$), matching the observed wall ratio ($\sim$20 vs.\ $\sim$7~min) implies $t_{\mathrm{MLP}}/t_{\mathrm{sim}}\approx 2.2$, so the simulator would need to be roughly $\mathbf{7\times}$ slower before a 33.5\% skip saves wall clock.

The same inequality, applied to maze micro-timings ($t_{\mathrm{BFS}}{\approx}0.34$~ms, $t_{\mathrm{MLP}}{\approx}0.95$~ms) and writing $t_{\mathrm{sim}}=t_{\mathrm{BFS}}+\delta$ for an injected per-eval delay $\delta$, yields a maze-specific break-even
\[
\delta^{\star}=\frac{t_{\mathrm{MLP}}-s\,t_{\mathrm{BFS}}}{s}.
\]
Under the observed skip rate of this wall study ($s{=}36.5\%\pm1.7\%$ at $2{,}500$ proposals; higher than the $27.3\%$ full-budget maze CPU operating point because early-trajectory proposals are easier for the gate to reject), $\delta^{\star}{\approx}2.3$~ms (${\approx}7.7\times$ bare BFS). A supplementary injected-delay run at $\delta{=}10$~ms ($n{=}10$; same maze domain and checkpoint; fitness labels unchanged) confirms the predicted wall win: genetic $28.3\pm 1.1$~s vs.\ genetic\_filter $22.1\pm 3.6$~s (mean $\Delta{=}{+}6.3$~s; filter faster on $9/10$ seeds; Wilcoxon one-sided $p{=}0.002$). This validates the break-even prediction under an artificially thickened evaluator within the same domain family, rather than demonstrating the effect on a naturally expensive simulator; the package is descriptive and does not amend maze H5 Holm.

A literature Sphere transfer of the same after-generation rule is appendix~\S\ref{sec:sphere-h2-appendix} (not required for the primary H2 claim).

\subsection{Why the after-generation surrogate wins (mechanism)}
\label{sec:why-filter}

The H2 gain is not a black box. Logged \texttt{surrogate\_archive.jsonl} rows for \texttt{genetic\_me\_filter} ($n{=}10$; $325{,}000$ proposal slots) show what the gate discards (Table~\ref{tab:skip-mech}). Live filter recomposes fitness at extinction gate~$0.95$. All skips cite \texttt{below\_fitness\_threshold} at $\tau{=}0.45$ (skip rate $33.5\%$). Skipped proposals have mean predicted fitness $0.21$ vs.\ $0.61$ for evaluated ones. $81\%$ of skips have predicted early-extinction probability $>0.8$ vs.\ $<0.1\%$ of evaluated proposals. Skipped diversity is low (mean $0.24$); evaluated diversity is high (mean $0.74$). Uncertainty barely separates the classes (medians both $\approx 0.16$). The operative signal is predicted fitness / extinction risk. Random emitters contribute disproportionately to skips ($49\%$ of random slots vs.\ $23\%$ of genetic). Among evaluated proposals, only $12.7\%$ realize true fitness $<0.45$ (pred--true MAE $0.066$).

The after-generation surrogate hard-constrains simulator spend. The before-generation surrogate only soft-advises an LLM under a JSON grammar; H1's flat matched contrast fits advice that is easy to ignore.

\textbf{Threshold sensitivity.} A sweep (Table~\ref{tab:h2-tau-sweep}) varies $\tau\in\{0.35,0.45,0.55\}$ ($n{=}10$ each). Skip rate scales monotonically ($24\%$ / $34\%$ / $51\%$). At $\tau{=}0.45$ filter leads 10/10 seeds at 15k and 20k evaluations. At $\tau{=}0.35$ mid-budget gain is smaller ($+2.5$~pp @15k, 9/10). At $\tau{=}0.55$ it is larger at 15k ($+5.4$~pp, 10/10) but terminal coverage falls further ($56.4\%$, paired $\Delta{=}{-}2.6$~pp). Aggressive gates trade terminal coverage for faster mid-budget fill.

\begin{table}[!htbp]
  \centering
  \caption{What the after-generation surrogate skips (\texttt{genetic\_me\_filter}, all seeds; descriptive).}
  \label{tab:skip-mech}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrr@{}}
    \toprule
    Quantity & Skipped & Evaluated \\
    \midrule
    Share of proposals & $33.5\%$ & $66.5\%$ \\
    Mean predicted fitness & $0.21$ & $0.61$ \\
    Mean pred.\ early-extinction $p$ & $0.90$ & $0.60$ \\
    Share with ext.\ $p>0.8$ & $81\%$ & $<0.1\%$ \\
    Mean predicted diversity & $0.24$ & $0.74$ \\
    Median uncertainty & $0.16$ & $0.16$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{Threshold sensitivity for \texttt{genetic\_me\_filter} vs.\ matched \texttt{genetic\_me\_uniform} ($n{=}10$ each; descriptive). Anytime $\Delta$ is filter $-$ uniform coverage at matched evaluation budget; terminal $\Delta$ at fixed 650 iterations.}
  \label{tab:h2-tau-sweep}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrrrrrr@{}}
    \toprule
    $\tau$ & $n$ & Skip & Terminal cov. & $\Delta$ @15k & $\Delta$ @20k & Terminal $\Delta$ \\
    \midrule
    0.35 & 10 & $24.4\%$ & $58.37\%$ & $+2.51$ (9/10) & $+2.67$ (9/10) & $-0.66$ \\
    0.45 & 10 & $33.5\%$ & $58.21\%$ & $+3.72$ (10/10) & $+3.67$ (10/10) & $-0.83$ \\
    0.55 & 10 & $50.7\%$ & $56.41\%$ & $+5.39$ (10/10) & $+2.86$ (10/10) & $-2.62$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\subsection{H3 --- LLM$+$filter stacking (boundary; appendix detail)}
\label{sec:h3}

Statuses (Table~\ref{tab:h-status}): \textbf{H3-prod = blocked}; \textbf{H3-gray = NI-pass-not-superiority}. Stacking the after-generation gate on the LLM$+$hint arm is \emph{not} a simple sum of H1$+$H2: production \texttt{filter} fails compose-gate skip-agreement ($d{=}0.528$; levels only, about $-1$~pp vs.\ hints), and a gray-zone repair passes NI margins at mean $\Delta$cov $-0.60$~pp without establishing superiority (Holm $m{=}3$). Full levels, diagnosis, and NI table: appendix~\S\ref{sec:h3-appendix}. Primary after-generation evidence remains H2; maze H5 shows the same filter-ports / LLM$+$filter-does-not pattern (\S\ref{sec:h5}).

\subsection{H4 --- Continuous-relaxation pyribs reference (CMA-ME / CMA-MAE)}
\label{sec:h4}

Pyribs baselines (five homogeneous CMA-ES emitters, $n=10$) provide a continuous-relaxation reference under the same warm-start and evaluation budget as other primary-grid arms. CMA searches $\mathbb{R}^{21}$ with \texttt{rint} decode (\S\ref{sec:pyribs}), so this is an upper-bound-style reference for continuous-friendly representations. Mean levels: cma\_me $73.37 \pm 3.65$\%, fitness $0.576 \pm 0.028$; cma\_mae $60.56 \pm 2.16$\%, fitness $0.489 \pm 0.007$; hints $60.41 \pm 0.82$\%, fitness $0.539 \pm 0.008$.

Decode / discrete-search controls (Bernoulli, native bit-flip, pbCMA) are appendix~\S\ref{sec:encoding-appendix}---not required to read H4 as a continuous-relaxation ceiling.

Paired hints $-$ cma\_me: $\Delta\text{cov} = -12.96 \pm 3.46$~pp (0/10 positive), $\Delta\text{fit} = -0.077 \pm 0.029$ (0/10); CMA-ME dominates coverage on every seed (largest gap at seed~4: $-20.0$~pp). Paired hints $-$ cma\_mae: $\Delta\text{cov} = -0.15 \pm 2.25$~pp (5/10), $\Delta\text{fit} = +0.010 \pm 0.008$ (10/10) --- near parity on coverage, with a small mean-fitness edge for hints.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{fig07_archive_heatmaps_seed4.pdf}
  \caption{Archive fitness in stability--diversity space: paired seed~4, LLM+scalar hints vs.\ CMA-ME (collapsed warm-start archives). Gray = empty niches; shared colour scale $[0,1]$. Coverage: hints 60.5\%, cma\_me 80.5\%. CMA-ME fills large low-diversity and mid-archive holes that remain empty under hints. Supplemental paired seeds with smaller/mid gaps (seeds~1, 6; $\Delta$cov $-9.4$ / $-16.0$~pp) and the three-seed panel: appendix Figure~\ref{fig:heatmap-panel}.}
  \label{fig:heatmaps}
\end{figure}

Figure~\ref{fig:heatmaps} shows where the $\approx$13--20~pp coverage gap lives on a representative seed: CMA-ME illuminates broad regions of behaviour space that hints leave empty---especially low-diversity bands and scattered interior holes---rather than only polishing already-filled high-fitness niches. Fitness colour is higher on average in the CMA panel, consistent with the fitness delta, but the dominant visual story is \emph{coverage}: gradient variation reaches niches the heterogeneous LLM stack does not. The gap is also visible along the evaluation axis (Figure~\ref{fig:anytime-ladder}): on seed~4, CMA-ME reaches $\sim$63\% coverage by 10{,}000 evaluations while hints are still $\sim$47\%.

\textbf{H4 outcome (fail; Table~\ref{tab:h-status}).} Holm $m=4$ (Table~\ref{tab:frq-ceiling}): hints do not beat \emph{both} pyribs arms simultaneously (only the CMA-MAE fitness contrast rejects). Continuous-relaxation CMA-ME leads hints by $\sim$13~pp on coverage (0/10)---a large wrong-direction effect, not an underpowered null. CMA-MAE coverage is near parity ($-0.15$~pp; fail-to-reject). This is an upper-bound-style continuous-relaxation reference, not a claim that every discrete CMA variant wins by the same margin (appendix~\S\ref{sec:encoding-appendix}).

\begin{table}[!htbp]
  \centering
  \caption{Pyribs continuous-relaxation reference (H4): Holm step-down ($m=4$, paired hints $-$ arm, one-sided greater). $\mathrm{PPS}$ = Kerby paired proportion of favorable pairs (Eq.~\eqref{eq:pps}).}
  \label{tab:frq-ceiling}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llrrcc@{}}
    \toprule
    Test & Arm / metric & Mean $\Delta$ & Raw $p$ & $\mathrm{PPS}$ & Holm reject \\
    \midrule
    H4\_mae\_fit & cma\_mae fitness & $+0.010$ & $9.8\times10^{-4}$ & 1.00 & \textbf{Yes} \\
    H4\_mae\_cov & cma\_mae coverage & $-0.15$~pp & 0.50 & 0.50 & No \\
    H4\_me\_cov & cma\_me coverage & $-12.96$~pp & 1.0 & 0.00 & No \\
    H4\_me\_fit & cma\_me fitness & $-0.077$ & 1.0 & 0.00 & No \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\subsection{Multi-LLM replication (boundary)}
\label{sec:multi-llm}

Bundled hints$-$stub replicates direction on DeepSeek V4 Pro and OpenAI \texttt{gpt-4o-mini} at $n{=}10$ (Holm $m{=}2$ per provider; never pooled). Absolute hint-arm coverage still differs by API ($\sim$62\% / $\sim$55\% vs.\ qwen-turbo $\sim$60\%). Matched-policy H1 remains the \texttt{qwen-turbo} TOST contrast. Full provider table: appendix~\S\ref{sec:multi-provider-appendix}.

\subsection{Cost and wall time (descriptive)}
\label{sec:cost}

Matched \emph{evaluation} budget (32{,}500 sims) is not matched dollar or wall cost (Table~\ref{tab:cost}). Mixed LLM arms issue 6{,}500 chat completions per seed ($\sim$90\% of wall is HTTP). At published \texttt{qwen-turbo} rates, API spend is on the order of $\sim\$1$--2 per seed ($\sim\$10$--20 for $n{=}10$); genetic and pyribs arms have \$0 API. We report call counts as the primary opaque budget.

On this cheap evaluator, the after-generation surrogate increases wall time despite fewer simulations. Maze LLM arms dominate wall ($\sim$6--7~h/seed at 32.5k proposals) vs.\ $<$1~min for genetic ME (Table~\ref{tab:b5}).

\textbf{Matched-dollar reading.} At near-equal coverage, \texttt{genetic\_me\_uniform} ($59.0\%$, $\sim$6~min, \$0) dominates \texttt{hints} ($60.4\%$, $\sim$94~min, $\sim$6.5k LLM calls) on wall and money. Continuous-relaxation CMA-ME reaches higher terminal coverage at moderate wall and \$0 API (\S\ref{sec:h4}).

\begin{table}[!htbp]
  \centering
  \caption{Mean cost per seed on the primary grid tier ($n=10$; descriptive). Wall from run summaries; LLM calls from \texttt{llm\_emit\_attempts}; API \$ is an order-of-magnitude estimate at published \texttt{qwen-turbo} intl.\ rates (see text), not an invoice.}
  \label{tab:cost}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    Condition & Wall (min) & LLM calls & Est.\ API (\$) \\
    \midrule
    \texttt{vanilla} & $4.4$ & 0 & 0 \\
    \texttt{genetic\_me\_uniform} & $6.3$ & 0 & 0 \\
    \texttt{genetic\_me\_filter} & $19.9$ & 0 & 0 \\
    \texttt{cma\_me} & $22.3$ & 0 & 0 \\
    \texttt{cma\_mae} & $23.4$ & 0 & 0 \\
    \texttt{stub} & $70.4$ & 6{,}500 & $\sim$1--2 \\
    \texttt{hints} & $93.6$ & 6{,}500 & $\sim$1--2 \\
    \texttt{filter} & $108.5$ & 6{,}500 & $\sim$1--2 \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\subsection{H5 --- Cross-domain after-generation check (maze)}
\label{sec:h5}
\label{sec:h5-maze}

\textbf{Scope.} Does the after-generation surrogate \emph{port} beyond the primary evaluator? Main-text H5 is the maze factorial ($n{=}10$; 32{,}500 proposals/arm; 900-cell grid; $\tau{=}0.685$ in a $\sim$25--45\% skip band). Confirmatory endpoints are coverage / QD-score AUC at matched real evaluations (Holm $m{=}8$). A parallel dungeon factorial (same design logic at 5k proposals) is appendix~\S\ref{sec:dungeon-appendix}---not required to follow the main H5 readout. Absolute $\tau$ values are domain-calibrated; the shared rule is the skip band (primary H2 sweep: Table~\ref{tab:h2-tau-sweep}).

Quality gates pass (pooled LLM fallback $0.009\%$; genetic\_filter skip $27.3 \pm 0.6\%$; parse success $\ge 99.89\%$). At fixed proposals (Table~\ref{tab:b5}): genetic arms saturate the 900-cell archive ($99.6 \pm 1.1\%$), while LLM arms plateau near half coverage (llm\_hints $47.7 \pm 3.3\%$). The terminal genetic$-$LLM gap ($\sim$52~pp) is a \textbf{ceiling artifact} plus emitter gap---not a fair terminal ranking. Descriptive hints$-$stub is near zero at the terminal ($+0.4$~pp).

\begin{table}[!htbp]
  \centering
  \caption{Maze mean levels at 32{,}500 proposals ($n{=}10$). Genetic arms hit a 900-cell ceiling; primary endpoints are AUC at matched evaluations (Table~\ref{tab:b5-holm}).}
  \label{tab:b5}
  \fitcenter{%
  \begin{tabular}{@{}lrrrr@{}}
    \toprule
    Arm & Coverage (\%) & QD-score & Skip (\%) & Wall (min) \\
    \midrule
    genetic & $99.6 \pm 1.1$ & 574 & 0 & $\sim$1 \\
    genetic\_filter & $99.6 \pm 1.1$ & 574 & 27.3 & $\sim$1 \\
    llm\_stub & $47.3 \pm 3.5$ & 250 & 0 & $\sim$377 \\
    llm\_hints & $47.7 \pm 3.3$ & 253 & 0 & $\sim$389 \\
    llm\_hints\_filter & $43.5 \pm 3.6$ & 230 & 27.9 & $\sim$424 \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

At matched real evaluations (22{,}860 evals; Table~\ref{tab:b5-holm}), genetic\_filter leads genetic on coverage AUC by $+4.7$~pp (10/10; Holm reject) and on QD-score AUC by $+30.8$ (10/10; Holm reject). Hints$-$stub coverage AUC is $+1.5$~pp (8/10; raw $p{=}0.065$; Holm non-reject). LLM$+$filter and acquisition interaction remain negative (4/10 and 0/10). Full five-arm Holm $m{=}8$ does \textbf{not} pass as a family (`family\_pass{=}False`)---\textbf{partial}: after-generation filter AUC ports; bundled LLM QD and interaction do not.

\textbf{Supplementary maze CPU} (genetic vs.\ genetic\_filter only; seeds $0$--$9$). Terminal coverage is saturated ($\sim$99.6\% both arms; skip $27.3\%$). On the evaluation axis at 23{,}300 matched evals, filter AUC rejects after Holm: coverage $+4.6$~pp (10/10) and QD-score $+30.2$ (10/10). Parallel dungeon CPU pairs and the locked dungeon five-arm family are appendix~\S\ref{sec:dungeon-appendix}.

\begin{table}[!htbp]
  \centering
  \caption{Maze Holm outcomes (main-text H5; $m{=}8$; AUC @ 22{,}860 real evaluations, $n{=}10$). Family does not pass ($2/8$ Holm rejects).}
  \label{tab:b5-holm}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrrc@{}}
    \toprule
    Contrast (AUC) & Mean $\Delta$ & Raw $p$ & Holm reject \\
    \midrule
    genetic\_filter $-$ genetic (coverage) & $+0.047$ & $9.8\times10^{-4}$ & Yes \\
    genetic\_filter $-$ genetic (QD-score) & $+30.8$ & $9.8\times10^{-4}$ & Yes \\
    hints $-$ stub (coverage) & $+0.015$ & 0.065 & No \\
    hints $-$ stub (QD-score) & $+10.0$ & 0.053 & No \\
    llm\_hints\_filter $-$ llm\_hints (coverage) & $-0.009$ & 0.862 & No \\
    llm\_hints\_filter $-$ llm\_hints (QD-score) & $-3.6$ & 0.839 & No \\
    acquisition interaction (coverage) & $-0.056$ & 1.0 & No \\
    acquisition interaction (QD-score) & $-34.3$ & 0.999 & No \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\textbf{H5 readout (maze).} After-generation sample-efficiency without LLM transfers at the calibrated maze $\tau$: genetic\_filter coverage / QD-score AUC Holm-reject ($n{=}10$). The five-arm family remains \textbf{partial} (hints / LLM$+$filter / interaction do not survive Holm). Bundled LLM QD lift does not transfer: genetic MAP-Elites leads LLM arms by $\sim$46~pp on maze coverage AUC ($35.5\%$ vs $81.7\%$ @ matched evals); the terminal genetic gap is inflated by the 900-cell ceiling. Alternate $\tau$ within maze is not tested here.

\section{Discussion}
\label{sec:discussion}

\subsection{Synthesis}

The takeaway is a soft/hard channel asymmetry under matched controls, not a renamed taxonomy. Hard after-generation intervention (H2) improves coverage \emph{per simulation} (Figure~\ref{fig:acquisition}); soft before-generation intervention (H1) stays within $\pm 2$~pp on \texttt{qwen-turbo}. Classical evolution-control vocabulary situates the design~\cite{jin2005fitness}; it does not by itself predict that asymmetry. CMA-ME (H4), multi-provider H1, and maze transfer (H5) bound the answer; Sphere / dungeon / encoding ablations are appendix material. Magnitudes are testbed-specific (\S\ref{sec:limitations}).

\subsection{Mechanistic reading}
\label{sec:why-discussion}

Results report \emph{what} moved (Fig.~\ref{fig:acquisition}); the skip logs and matched contrasts suggest a candidate \emph{why}.

\begin{itemize}
  \item \textbf{Budget reallocation.}
 Skip logs (\S\ref{sec:why-filter}) show discarded proposals at low predicted fitness (mean $0.21$ vs.\ $0.61$), high extinction risk ($81\%$ with $p>0.8$), and low diversity. Uncertainty barely separates skip from evaluate. At matched evaluations that discarded mass frees slots ($+3.65$~pp @ 20k); at fixed iterations the same gate finishes $-0.83$~pp.
  \item \textbf{Shared checkpoint, asymmetric use.}
 Offline hold-out at gate~$0.95$ gives $R^2_{\mathrm{fitness}}{=}0.94$ with NMAE$=0.112$ (appendix~\S\ref{sec:checkpoint-appendix})---worse absolute error than the historical gate-$0.5$ NMAE$=0.052$. Live skip logs still separate skip vs.\ evaluate by predicted fitness / extinction (Table~\ref{tab:skip-mech}); false-accept mass among evaluated proposals is $12.7\%$. The same predictor feeds H1 and yields a flat matched contrast, so attach role matters more than headline $R^2$.
  \item \textbf{Soft vs.\ hard channel.}
 Before-generation advises; after-generation constrains spend. Matched H1 stays within $\pm 2$~pp; enriched pilots stay within $|\Delta\mathrm{cov}|<2$~pp. Prompt-side scalars are easy to ignore on this stack.
  \item \textbf{Warm-start context.}
 Confirmatory arms warm-start from the same $\sim$2{,}614-elite baseline. H2 mid-budget gains arise inside an already seeded archive. Whether before-generation advice would matter more from a cold start remains open (\S\ref{sec:bootstrap-hyp}).
  \item \textbf{Threshold as a design knob.}
 Primary H2 uses $\tau{=}0.45$; the sweep at $\{0.35,0.45,0.55\}$ shows mid-budget gains scale with skip rate while aggressive $\tau$ hurts terminal coverage (Table~\ref{tab:h2-tau-sweep}).
  \item \textbf{Prompt numeracy in this stack.}
 Before-generation scalars do not move coverage here. Prompt design, grammar, model choice, and mid-band saturation remain plausible modulators.
\end{itemize}

\textbf{Working interpretation.} The same checkpoint yields asymmetric payoffs by attach role. After-generation reallocates simulations by filtering predicted low-fitness / high-extinction / low-diversity proposals. Before-generation leaves those predictions as soft advice and stays flat. The $\tau$ sweep is consistent with a mid-budget effect that is not locked to one gate setting. Magnitudes are testbed-specific.

\subsection{Boundary conditions}

Several contrasts sharpen what the positive H2 reading covers. Matched H1 is flat on \texttt{qwen-turbo} (\S\ref{sec:decomposition}). Multi-provider runs reproduce the bundled $\Delta$ direction. Relative to matched genetic ME, LLM+\texttt{hints} adds only $\approx{+}1.4$~pp at roughly $10\times$ wall time.

Under continuous-relaxation encoding, CMA-ME leads hints by $\approx 13$~pp; CMA-MAE is near parity on coverage (decode / discrete-search controls: appendix~\S\ref{sec:encoding-appendix}). Stacking the after-generation gate under LLM does not improve fixed-iteration coverage on locked production \texttt{filter} (about $-1$~pp); gray-zone repair narrows but does not reverse the gap (confirmatory mean $-0.60$~pp; \S\ref{sec:h3}). On maze (H5), genetic MAP-Elites leads all LLM arms while after-generation sample-efficiency still ports; the genetic terminal gap is inflated by a 900-cell ceiling---compare matched-eval AUC.

\subsection{Implications for LLM emitters}
\label{sec:when-llm}

Section~\ref{sec:intro} already scopes the paper as an EC model-management question, not an LLM product: the object is where a shared surrogate attaches, with the LLM as one concrete emitter. Consistent with that scope---not as a late pivot---if the decision criterion is coverage, wall time, or API dollars on this evaluator, an LLM emitter is mostly not the metrics winner: continuous-relaxation CMA-ME leads LLM+\texttt{hints} by $\sim$13~pp (H4); matched-policy genetic ME is within $\approx{+}1.4$~pp of hints at roughly $10\times$ lower wall and \$0 API (\S\ref{sec:cost}, Figure~\ref{fig:ladder}). Maze H5 reverses the LLM vs.\ genetic coverage ranking. That ranking bounds the attach-point story; it does not replace it. A remaining niche is \emph{structured, human-readable variation}: accepted LLM elites show more compact named-field edits than genetic bit noise (edit-anatomy case study, appendix~\S\ref{sec:edit-anatomy}). Practical reading: use an LLM when editable structured genotypes are a product requirement; otherwise prefer genetic$+$after-generation (or a CMA-style baseline) for archive metrics, and attach any shared surrogate primarily after generation if an LLM is retained.

\subsection{Hypothesis: stage-dependent integration}
\label{sec:bootstrap-hyp}

The fixed-slot decomposition motivates an \emph{untested} scheduling hypothesis---a consequence of the where-question, not a co-equal claim of this paper:

\begin{quote}
\textit{The before-generation surrogate is most useful during exploration / archive bootstrapping; the after-generation surrogate remains beneficial across the optimization trajectory on the evaluation axis.}
\end{quote}

Evidence consistent with that reading (not a test of it):

\begin{itemize}
  \item \textbf{After-generation across the trajectory.}
 On H2 anytime curves (Figure~\ref{fig:acquisition}), \texttt{genetic\_me\_filter} leads \texttt{genetic\_me\_uniform} from early through mid budgets (about $+1.4$~pp @ 5k evaluations; $+3.0$ @ 10k; peak $\sim$+$3.7$ @ 15k--20k; 10/10 seeds), while skip rates stay high in early/mid/late iteration terciles ($\sim$37\% / 33\% / 31\%). That pattern suggests a mid-trajectory sample-efficiency advantage rather than a cold-start-only effect; by the full 32{,}500-evaluation endpoint the filter is slightly behind on coverage ($-0.8$~pp).
  \item \textbf{Before-generation early.}
 Terminal matched-policy H1 is flat within $\pm 2$~pp ($n{=}10$), which is compatible with either little prompt-side value overall or value concentrated in a phase we did not instrument. Soft-vs-hard channel asymmetry (\S\ref{sec:why-discussion}) and the usual MAP-Elites bootstrap/refine split make an early-only reading plausible. Frozen LLM arms lack \texttt{archive\_trace.jsonl}, so an early lift---if present---is not yet localized. Terminal elite histograms show low-fitness mass in filled niches, but as static snapshots they cannot date niche discovery.
  \item \textbf{Emitter scheduling.}
 Terminal mid-band parity (genetic ME $\approx$ LLM+\texttt{hints}) also invites LLM-for-bootstrap / genetics-for-refinement schedules; equally, genetics may simply catch up by iteration~650, or LLM value may be BC-region-specific.
\end{itemize}

Stage-dependent integration is a hypothesis motivated by the observed decomposition, not a second research question of this paper. Our fixed attach-role contrasts ask \emph{where} the surrogate should attach; they naturally suggest a future question: \emph{when} should each attach role be active? Follow-ons include anytime first-fill curves for hints vs.\ stub\_uniform, filter on/off by phase, and LLM for the first $K$ iterations then genetics.

\subsection{Limitations}
\label{sec:limitations}

\subsubsection{Threats to validity}

Primary evidence uses a cheap evaluator where MLP inference dominates wall time, so H2's evaluation-indexed gain is not a wall-clock win on the unmodified primary or maze BFS evaluators (\S\ref{sec:cost}). Wall-clock transfer above the predicted break-even is now empirically validated on maze via injected delay (\S\ref{sec:h2}); transfer to a naturally expensive simulator remains untested. The pyribs coverage contrast searches a continuous relaxation of a mixed discrete/continuous genome; decode ablations still propose in $\mathbb{R}^{21}$ before rounding, and the CMA comparison is a pipeline reference rather than a factorial emitter decomposition (\S\ref{sec:h4}). Matched evaluation budget is not matched dollar cost.

H3 statuses remain Table~\ref{tab:h-status}: H3-prod \textbf{blocked}; H3-gray \textbf{NI-pass-not-superiority} (mean $\Delta$cov $-0.60$~pp). Hold-out gate alignment is appendix~\S\ref{sec:checkpoint-appendix}.

LLM endpoints are window-pinned with \texttt{llm\_spec\_hash}, but floating aliases limit bit-exact replay; genetic ME and pyribs CMA remain locally re-runnable. Holm is within locked contrast sets; paper-wide Holm agrees on claim-driving rows (Table~\ref{tab:paper-holm}). Only matched H1 carries a formal TOST/MDE. Other non-rejects are fail-to-reject, not equivalence claims: maze hints$-$stub and LLM$+$filter are small or wrong-direction; H4's CMA-ME miss is a large reverse effect. Maze five-arm Holm $m{=}8$ is complete at $n{=}10$ but remains \textbf{partial} (filter AUC Holm-rejects; family does not pass).

\subsubsection{Generalizability}

Primary contrasts use a controlled structured-genotype evaluator (\S\ref{sec:substrate}) chosen so that both soft prompt-side and hard after-generation interventions are definable. Maze is the main-text discrete PCG transfer domain; magnitudes ($+3.7$~pp, $33.5\%$ skips) remain testbed-specific. We do not claim a universal ranking on expensive continuous genomes or CFD-style SAIL domains. A literature Sphere H2 check and a dungeon factorial are appendix~\S\ref{sec:sphere-h2-appendix} / \S\ref{sec:dungeon-appendix} (not required for the primary H1/H2/H5 maze claims). Maze genetic arms hit a 900-cell ceiling; secondary-domain $\tau$ was set by shadow replay, not by repeating the primary online sweep (Table~\ref{tab:h2-tau-sweep}). Multi-provider rows replicate the bundled $\Delta$ direction at $n{=}10$; absolute coverage still differs by API (matched H1 remains \texttt{qwen-turbo}).

\subsubsection{Scope of this paper}

Analysis rules are frozen per confirmatory family in dated repository protocol files, not as a public OSF/Zenodo stamp of the whole manuscript (amendment kinds and commit$\leftrightarrow$run timeline: appendix~\S\ref{sec:amendments-appendix}). Beyond primary H1/H2, main text reports H4 as a continuous-relaxation boundary and maze H5 (partial). H3 stacking, multi-provider bundled rows, Sphere, dungeon, and CMA encoding sit in the appendix / journal-extension track. Still open: full-budget LLM dungeon, maze $\tau$ ablations, and DSA-ME-style deckbuilding H1+H2 (Future work).

\subsection{Future work}

What remains open is less about new LLMs or domains than about mechanism timing (\S\ref{sec:why-discussion}): (1)~\textbf{phase analysis}---extend anytime curves to genetic ME and mid-trajectory H2 vs.\ hints; (2)~\textbf{prompt micro-ablation} (drop uncertainty / predicted fitness / archive stats) to refine the soft-channel reading of H1. H2 threshold sensitivity at $\tau\in\{0.35,0.45,0.55\}$ ($n{=}10$) is reported in Table~\ref{tab:h2-tau-sweep}; a wider $\tau$ grid remains optional.

The fixed attach-role contrasts (Table~\ref{tab:h-status}) leave open a follow-on question: when should before- vs.\ after-generation be active, and when to call LLM vs.\ genetic emitters (\S\ref{sec:bootstrap-hyp})? That is future work, not a claim of this paper. Planned journal extensions: (i)~a recognized LLM+QD domain (DSA-ME-style deckbuilding~\cite{grillotti2022dsame}) to test whether the flat H1 result and positive H2 result replicate under a structured discrete genome with an established after-generation gate baseline; (ii)~full-budget LLM dungeon, maze $\tau$ ablations, and fuller treatment of the appendix dungeon / Sphere / encoding packages; (iii)~optional expert preference pilot on the same parent$\rightarrow$child pairs used in the edit-anatomy case study (\S\ref{sec:edit-anatomy}). An external Zenodo/OSF snapshot of the frozen protocol files and claim-driving artifacts is the natural next integrity step once confirmatory compute for this revision is closed (not claimed as a pre-run stamp for past families).

\section{Conclusion}
\label{sec:conclusion}

Previous work often compared QD pipelines; we compared soft vs.\ hard uses of one shared surrogate in an LLM-guided MAP-Elites loop. The non-obvious result is channel asymmetry: after-generation gating yields ${\sim}3.7$~pp mid-budget coverage per simulation and $33.5\%$ fewer sims at fixed iterations (but $-0.83$~pp and slower wall time on this evaluator), while matched before-generation scalars stay within $\pm 2$~pp (TOST). Genetic ME sits within about $1.4$~pp of LLM+\texttt{hints}; continuous-relaxation CMA-ME leads by $\sim$13~pp (\S\ref{sec:h4}). Maze H5 is \textbf{partial} (Table~\ref{tab:h-status}). Code and artifacts are released at tag~\texttt{\SubmissionGitTag} / commit~\texttt{\SubmissionGitShort}. Scope: \S\ref{sec:limitations}.

\section*{Data and code availability}

{\sloppy
\textbf{Code.} Source, scheduler YAMLs, analysis scripts, locked protocol files, and experiment artifacts are in the public repository \url{https://github.com/Starovoitov/LifeManifold}. Intended submission pin: tag~\texttt{\SubmissionGitTag}, short SHA~\texttt{\SubmissionGitShort}, full SHA~\texttt{\SubmissionGitFull} (fill at upload on the commit that produced the PDF).

\textbf{Protocols.} Confirmatory families are frozen per analysis window in dated repository protocol files (primary grid; maze H5; appendix dungeon), not as an external OSF/Zenodo pre-registration of the entire paper. Amendment kinds (lock / extension / reporting) and a commit$\leftrightarrow$artifact timeline are appendix~\S\ref{sec:amendments-appendix}; full dual-report wording lives with the protocol files in the repository.

\textbf{Artifacts and reproduction.} Per-seed archives, aggregated summaries, surrogate checkpoints, and offline replay JSON used in the appendices are under \texttt{artifacts/} in that repository. Batch and statistics entry points, including tier names and Holm-family flags, are documented in the repository README / protocol files rather than repeated here.
\par}

\section*{Declaration of generative AI and AI-assisted technologies in the writing process}
During the preparation of this work the author used Cursor (Composer) to draft and revise manuscript text and LaTeX formatting, and to assist with implementation and analysis code in the accompanying repository. After using this tool, the author reviewed and edited the content as needed and takes full responsibility for the content of the article and the correctness of the code. Large language models used as MAP-Elites emitters in the experiments (e.g.\ \texttt{qwen-turbo} and provider replications) are part of the study method and are described in Sections~\ref{sec:method}--\ref{sec:results}; they are not AI-assisted writing tools for this declaration.

\bibliographystyle{elsarticle-num}
\bibliography{references}

\appendix
\FloatBarrier

\section{Statistical details}
\label{sec:stats-appendix}

Main-text reporting uses paired Wilcoxon tests and within-set Holm (\S\ref{sec:stats}). This appendix records effect-size conventions (including the Kerby $\mathrm{PPS}$ abbreviation) and the paper-wide Holm check.

\paragraph{Multiple-comparison policy.}
Primary decisions use Holm within each pre-specified confirmatory contrast set ($\alpha{=}0.05$). Sets are frozen per family relative to that family's analysis window (bundled stack; H4; maze H5 AUC; per-provider replications; gray-zone NI); matched H1 TOST sits outside primary-grid confirmatory Holm sets. Dungeon AUC is the parallel appendix family. As a robustness check, a paper-wide Holm step-down on the pooled primary-grid raw $p$-vector ($m{=}20$; appendix Table~\ref{tab:paper-holm}) leaves every claim-driving reject/non-reject unchanged (\textbf{11/20} reject). Primary outcomes remain Table~\ref{tab:families}.

\paragraph{Paired proportion of favorable pairs ($\mathrm{PPS}$).}
Classic Vargha--Delaney $A_{12}$ is defined for independent samples~\cite{vargha2000new}. On paired seeds we report Kerby's \emph{paired proportion of favorable pairs}~\cite{kerby2014simple} (common-language effect size for matched pairs~\cite{mcgraw1992cl}). The short label $\mathrm{PPS}$ is only a table abbreviation for that established quantity---not a new estimand and not a propensity score:
\begin{equation}
  \mathrm{PPS} \;=\; \frac{\#\{\Delta_i > 0\} + 0.5\,\#\{\Delta_i = 0\}}{n},
  \label{eq:pps}
\end{equation}
where $\Delta_i$ is treatment $-$ control on seed $i$ (for ``less,'' count $\Delta_i < 0$). $\mathrm{PPS}{=}0.5$ is chance under no systematic paired advantage, and (with ties as half) $\mathrm{PPS}$ coincides with the Sign column rate when there are no ties. We use it only as a descriptive companion next to Wilcoxon tests. We do \emph{not} import independent-sample $A_{12}$ magnitude cutoffs (e.g.\ 0.56/0.64/0.71), and $\mathrm{PPS}$ is \emph{not} a confirmatory gate (Holm / protocol $d_z$ decide pass/fail).

\paragraph{H1 TOST and MDE; retrospective MDEs elsewhere.}
Matched-policy H1 uses a post-hoc TOST equivalence check at $|\Delta\mathrm{cov}|\le 2$~pp (outside confirmatory Holm sets). Approximate minimum detectable effect for a paired $t$-test at $n$ seeds, two-sided $\alpha{=}0.05$, 80\% power is
\begin{equation}
  \mathrm{MDE} \;=\; \bigl(t_{1-\alpha/2,\,n-1} + t_{0.80,\,n-1}\bigr)\,\frac{s_{\Delta}}{\sqrt{n}},
  \label{eq:mde}
\end{equation}
with $s_{\Delta}$ the observed paired-difference SD (descriptive when used retrospectively). For H1, $\mathrm{MDE}{\approx}1.2$~pp at $n{=}10$; accept/reject and CIs appear in \S\ref{sec:decomposition}. Elsewhere we quote only the resulting MDE numbers under the same convention (e.g.\ dungeon hints$-$stub coverage AUC ${\approx}1.2$~pp at $n{=}10$; maze genetic\_filter coverage AUC ${\approx}2.1$~pp at $n{=}10$ before Holm $m{=}8$)---not a second formula statement. These bound what $n$ can detect; they do not re-open locked Holm families or convert non-rejects into equivalence claims.

\paragraph{Other descriptive metrics.}
QD-score (Eq.~\eqref{eq:qd}), evaluation count, wall-clock time, LLM fallback rate, anytime curves, and bootstrap 95\% CIs on median $\Delta$ are reported for interpretation. QD-score and eval-to-threshold curves are \emph{not} used for confirmatory rejection. Hold-out surrogate CIs use $B{=}10{,}000$ row resamples; cross-gate comparisons prefer NMAE when the fitness target distribution changes (appendix~\S\ref{sec:checkpoint-appendix}).

\section{Shared-checkpoint validity (hold-out algebra)}
\label{sec:checkpoint-appendix}
\label{sec:checkpoint}

Main text cites only the runtime-aligned headline ($R^2{=}0.94$, NMAE$=0.112$). This appendix records why $R^2$ and MAE can move in opposite directions when the compose gate changes.

Offline training on the nightly buffer ($N=35{,}114$ rows; schema~2.1, 24-D features; hold-out $n{=}7{,}023$) yields hold-out metrics for checkpoint \texttt{nightly\_v3\_mc\_d005.pkl}. Protocol amendment 2026-07-28 aligns the offline compose gate with runtime YAML ($0.95$): $R^2_{\text{fitness}}=0.94$ (bootstrap 95\% CI $[0.94, 0.95]$), MAE$_{\text{fitness}}=0.029$ ($[0.027, 0.030]$), NMAE$=\mathrm{MAE}/\mathrm{std}(y)=0.112$ ($[0.107, 0.117]$), MAE$_{\text{stability}}=0.040$, passing \texttt{hints\_ok}.

At gate~$0.5$, $96.2\%$ of hold-out labels are exactly zero (std$(y){=}0.133$); at gate~$0.95$, only $29.0\%$ are zero and the continuous mass spreads (std$(y){=}0.255$, $\times 1.92$). Because $R^2{=}1{-}\mathrm{SS}_{\mathrm{res}}/\mathrm{SS}_{\mathrm{tot}}$, larger target variance inflates $R^2$ even as absolute MAE grows ($\times 4.12$). Scale-normalized error worsens (NMAE $0.052\!\rightarrow\!0.112$). Therefore $R^2{=}0.94$ is \emph{not} a model-quality upgrade over the historical $R^2{=}0.76$ headline---it is the correct metric for the runtime target (artifact: \texttt{holdout\_compose\_gate\_alignment.json}). The misalignment affected only the offline validity metric; live filter logs always used gate~$0.95$. Hold-out metrics do not replace simulator endpoints for H1/H2/H3.

\section{MLP vs.\ GP/UCB ablation (offline)}
\label{sec:surrogate-ablation-appendix}

\emph{Not required for the primary H2 claim.} Production H2 uses an MLP ensemble with MC-dropout and a fitness threshold ($\texttt{min\_fit}{=}0.45$, $\texttt{max\_u}{=}1.0$), not SAIL-style GP+UCB. This appendix records the offline MLP-vs-GP/UCB comparison that motivates that choice. On the nightly buffer hold-out at runtime-aligned compose gate~$0.95$, the production MLP achieves $R^2{=}0.94$ (NMAE$=0.112$) vs.\ $0.89$ (NMAE$=0.222$) for a scikit-learn GP fitness regressor trained on 5{,}000 rows under the \emph{same} label definition, and is ${\sim}37\times$ faster at a 32{,}500-proposal batch. An earlier M1 snapshot at misaligned gate~$0.5$ reported MLP $R^2{=}0.76$ vs.\ GP ${\approx}0.22$; that gap is not reusable as a model-quality argument because ${\approx}96\%$ of gate-$0.5$ labels are zero (\S\ref{sec:checkpoint-appendix}).

Offline acquisition replay: replacing the MLP with the GP under the \emph{same} threshold gate worsens the false-skip proxy ($0.7\%\!\rightarrow\!3.2\%$); a UCB skip rule on GP scores ($\mu{+}\beta\sigma < \texttt{min\_fit}$, $\beta\in\{0.15,0.5,1.0\}$) stays near $99\%$ skip. On logged Q1 filter proposals ($n{=}325{,}000$), replaying UCB on the \emph{same} MLP $\mu/\sigma$ only softens the gate (skip $33.5\%\!\rightarrow\!28.5\%/25.7\%/21.2\%$) and never adds skips. With $\texttt{max\_u}{=}1.0$, uncertainty is not load-bearing for the production threshold gate. GP+UCB is therefore a related SAIL lineage, not a competitive drop-in.

\section{Multi-provider bundled replication}
\label{sec:multi-provider-appendix}

\emph{Not required for matched-policy H1.} Main-text \S\ref{sec:multi-llm} summarizes direction-only replication. Table~\ref{tab:g1} keeps the historical stub arm (stack $\Delta$, not matched H1). Never pool across APIs.

\begin{table}[!htbp]
  \centering
  \caption{Multi-provider bundled hints $-$ stub by LLM (separate experiments; never pooled). Absolute hint-arm coverage shows level gaps across APIs.}
  \label{tab:g1}
  \fitcenter{%
  \begin{tabular}{@{}lccccl@{}}
    \toprule
    Provider & Hints cov.\ (\%) & $\Delta$cov (pp) & $\Delta$fit & Sign (cov) & Outcome \\
    \midrule
    qwen-turbo (v2 reference, $n{=}10$) & $60.41$ & $+15.09$ & $+0.053$ & 10/10 & passes \\
    DeepSeek V4 Pro ($n{=}10$) & $61.93$ & $+16.95 \pm 2.12$ & $+0.052 \pm 0.013$ & 10/10 & passes \\
    OpenAI gpt-4o-mini ($n{=}10$) & $55.34$ & $+9.66 \pm 2.41$ & $+0.018 \pm 0.013$ & 10/10 & passes \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

DeepSeek matches the qwen-turbo reference in magnitude and absolute hint-arm coverage ($\sim$62\% vs.\ $\sim$60\%). gpt-4o-mini replicates direction at full power ($p_{\mathrm{cov}}{=}0.001$, $p_{\mathrm{fit}}{=}0.003$ at $n{=}10$) but absolute hint-arm coverage is lower ($55.3\%$); stub arms align across providers ($\sim$45\% coverage). Averaging the three $\Delta$cov rows would be methodologically wrong.

\section{Literature Sphere H2 transfer}
\label{sec:sphere-h2-appendix}

\emph{Not required for the primary H2 claim.} Main-text \S\ref{sec:h2} isolates after-generation attachment on the primary CA. This appendix records a Fontaine linear-projection Sphere check of the same threshold-gate rule.

The same after-generation rule---threshold gate plus empty-bin force-eval---on Fontaine linear-projection Sphere ($D{=}20$; MAP-Elites + isotropic Gaussian; no LLM) reproduces the evaluation-indexed pattern (Table~\ref{tab:sphere-h2}): $+7.88$~pp coverage at matched evaluations (10/10) with $\sim$38\% skips and near-parity terminal coverage at fixed proposals. \textbf{What Sphere validates:} H2-style evaluation gating on a recognized continuous QD benchmark. \textbf{What it does not:} H1 (no named fields for prompt scalars), LLM emitters, or CFD-scale absolute numbers. Descriptive Wilcoxon only---not a confirmatory Holm family. Repository CMA Sphere/Rastrigin tiers remain pyribs-runner sanity checks.

\begin{table}[!htbp]
  \centering
  \caption{Supplementary Sphere H2: matched \texttt{me\_uniform} vs.\ \texttt{me\_filter} (Fontaine linear-projection Sphere; $n{=}10$; 32{,}500 proposals). Descriptive Wilcoxon only---not a confirmatory Holm family.}
  \label{tab:sphere-h2}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lr@{}}
    \toprule
    Quantity & Value \\
    \midrule
    Mean skip rate & $37.9\% \pm 2.7$ \\
    Mean true evals (filter) & $20{,}178$ \\
    Terminal $\Delta$cov (filter $-$ uniform @ fixed proposals) & $+0.18$~pp (5/10) \\
    Matched-eval $\Delta$cov @ $18{,}699$ evals & $+7.88$~pp (10/10; $p{=}9.8\times10^{-4}$) \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\section{CMA encoding / discrete-search controls}
\label{sec:encoding-appendix}

\emph{Not required to read main-text H4 as a continuous-relaxation ceiling.} Main-text \S\ref{sec:h4} reports \texttt{rint}-decode CMA-ME/MAE vs.\ hints. This appendix records decode and discrete-search controls that bound how much of that gap is encoding vs.\ search.

\textbf{Encoding ablation} (seeds~0--4, descriptive): alternate rule-bit \emph{decode} and cold-start CMA-ME leave the continuous-relaxation gap intact. These runs vary how continuous proposals map to rule bits; they do \emph{not} test discrete search in the same space as genetic/LLM emitters. \texttt{threshold} ($\geq 0.5$ bits) is \emph{bit-identical} to frozen \texttt{rint} on every seed; \texttt{bernoulli} decode is $-0.46$~pp mean ($72.18\%$); empty-archive \texttt{cold} is $-3.47$~pp ($69.17\%$)---still $\geq +8.8$~pp above hints on every arm mean.

\textbf{Bernoulli-decode CMA-ME} ($n{=}10$, descriptive). Same warm-start and 32{,}500-evaluation budget; CMA proposes in $\mathbb{R}^{21}$ but rule bits are Bernoulli-sampled at decode (discrete genotypes at evaluation, continuous covariance adaptation unchanged). Mean coverage $71.25 \pm 2.88$\%, fitness $0.578 \pm 0.020$. Paired vs.\ \texttt{hints}: $+10.84 \pm 2.94$~pp (10/10 seeds); vs.\ \texttt{genetic\_me\_uniform}: $+12.22 \pm 2.56$~pp (10/10); vs.\ \texttt{rint} CMA-ME: $-2.12 \pm 4.93$~pp (3/10).

\textbf{Native discrete-search CMA-ME} ($n{=}10$, descriptive). Custom bit-flip emitter on rule bits + Gaussian on floats, discrete $\{0,1\}$ genotype stored in archive, $\sigma$ adapted by a 1/5 success rule (MVP). Mean coverage $65.57 \pm 3.40$\%, fitness $0.542 \pm 0.028$. Paired vs.\ \texttt{hints}: $+5.16 \pm 3.67$~pp (10/10); vs.\ \texttt{genetic\_me\_uniform}: $+6.54 \pm 3.76$~pp (10/10); vs.\ Bernoulli: $-5.68 \pm 5.90$~pp (1/10); vs.\ \texttt{rint}: $-7.80 \pm 5.07$~pp (0/10).

\textbf{pbCMA CMA-ME} ($n{=}10$, descriptive). Latent $(\mu/\mu_w,\lambda)$-CMA on $\mathbb{R}^{21}$ with bit threshold at $0.5$, margin correction, and CMA updates from \emph{latent} ranked parents (discrete $\{0,1\}$ genotype stored in archive). Mean coverage $69.95 \pm 5.42$\%, fitness $0.548 \pm 0.034$. Paired vs.\ \texttt{hints}: $+9.54 \pm 5.25$~pp (10/10); vs.\ \texttt{genetic\_me\_uniform}: $+10.92 \pm 5.80$~pp (10/10); vs.\ native bit-flip: $+4.38 \pm 5.93$~pp (8/10); vs.\ Bernoulli: $-1.30 \pm 6.54$~pp (4/10); vs.\ \texttt{rint}: $-3.42 \pm 7.12$~pp (2/10). Latent covariance adaptation recovers most of the MVP bit-flip deficit vs.\ Bernoulli and nearly half the \texttt{rint} gap, but continuous-relaxation \texttt{rint} still leads on eight seeds---consistent with a modest residual encoding advantage, not ``any CMA-ME pipeline wins.''

\section{Dungeon factorial (supplementary H5)}
\label{sec:dungeon-appendix}

\emph{Not required for the main-text H5 maze readout.} A locked 16$\times$16 patch-LLM dungeon under frozen v4 ($n{=}10$, 5{,}000 proposals/arm) asks the same transfer question on a confirmatory AUC family. Quality gates passed (LLM fallback $\le 0.99\%$, filter skip 34--40\%). Filter $\tau{=}0.78$ (skip-band calibration). Figure~\ref{fig:b4} shows anytime curves.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.48\linewidth]{fig_b4_anytime_coverage.png}
  \hfill
  \includegraphics[width=0.48\linewidth]{fig_b4_anytime_qd_score.png}
  \caption{Dungeon domain anytime curves (5k proposals/arm, $n=10$ seeds): coverage (left) and QD-score (right).}
  \label{fig:b4}
\end{figure}

At fixed proposals (Table~\ref{tab:b4}): local genetic MAP-Elites dominates all LLM arms ($54.82 \pm 3.11$\% vs.\ llm\_hints $35.60 \pm 2.21$\%; $+19.2$~pp, 10/10). Mean elite fitness is flat ($\sim$0.73--0.74); gains are coverage-driven. Descriptive hints $-$ stub: $+2.31$~pp (9/10). Filter arms finish lower at fixed proposals (genetic\_filter $-4.4$~pp; llm\_hints\_filter $-5.4$~pp vs.\ unfiltered).

\begin{table}[!htbp]
  \centering
  \caption{Dungeon mean levels at 5{,}000 proposals ($n=10$).}
  \label{tab:b4}
  \fitcenter{%
  \begin{tabular}{@{}lrrrr@{}}
    \toprule
    Arm & Coverage (\%) & Mean fit & QD-score & Skip (\%) \\
    \midrule
    genetic & $54.82 \pm 3.11$ & 0.738 & 364 & 0 \\
    genetic\_filter & $50.39 \pm 3.13$ & 0.739 & 335 & 36.5 \\
    llm\_stub & $33.29 \pm 1.97$ & 0.734 & 220 & 0 \\
    llm\_hints & $35.60 \pm 2.21$ & 0.735 & 235 & 0 \\
    llm\_hints\_filter & $30.22 \pm 2.25$ & 0.735 & 200 & 37.1 \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

At a matched real-evaluation checkpoint (2{,}991 evals), genetic\_filter beats genetic by $+7.83$~pp (10/10); hints beats stub by $+1.52$~pp (8/10). Locked dungeon H5 is \textbf{partial} (Holm $m=8$, AUC at matched budget; Table~\ref{tab:b4-holm}): genetic\_filter $-$ genetic coverage and QD-score AUC pass; hints $-$ stub and LLM+filter interaction do not survive correction. Genetic MAP-Elites remains the strong dungeon baseline ($\sim$19~pp above LLM hints at fixed proposals).

\textbf{Supplementary CPU @ 32{,}500 proposals} ($n{=}10$; not part of the locked dungeon H5 AUC set). At fixed proposal slots both dungeon arms nearly saturate ($89.42 \pm 0.77$\% genetic vs.\ $88.79 \pm 1.75$\% genetic\_filter; $-0.63$~pp), while filter skips $30.8\%$ of slots. On the evaluation axis, filter AUC still leads at matched budget (Table~\ref{tab:h2-cpu-full}): coverage AUC $+3.6$~pp (9/10; Holm reject) and QD-score AUC $+33.9$ (8/10; Holm reject).

\textbf{Dungeon filter mechanism (descriptive).} Skip logs on \texttt{genetic\_filter} ($325{,}000$ proposal slots @ 32.5k) discard low predicted fitness at low uncertainty (mean pred.\ fitness $0.71$ skipped vs.\ $0.63$ evaluated; mean uncertainty $0.03$ vs.\ $0.08$); all skips cite \texttt{below\_fitness\_threshold}. Empty bins are still explored ($\lesssim$2\% of evaluated rows @ 5k).

\begin{table}[!htbp]
  \centering
  \caption{Dungeon Holm outcomes ($m{=}8$; AUC @ 2{,}991 real evaluations, $n=10$). Only the two genetic\_filter contrasts reject after Holm.}
  \label{tab:b4-holm}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrrc@{}}
    \toprule
    Contrast (AUC) & Mean $\Delta$ & Raw $p$ & Holm reject \\
    \midrule
    genetic\_filter $-$ genetic (coverage) & $+0.048$ & $9.8\times10^{-4}$ & \textbf{Yes} \\
    genetic\_filter $-$ genetic (QD-score) & $+29.9$ & $9.8\times10^{-4}$ & \textbf{Yes} \\
    hints $-$ stub (coverage) & $+0.008$ & 0.032 & No \\
    hints $-$ stub (QD-score) & $+5.3$ & 0.019 & No \\
    llm\_hints\_filter $-$ llm\_hints (coverage) & $\approx 0$ & 0.50 & No \\
    llm\_hints\_filter $-$ llm\_hints (QD-score) & $-0.77$ & 0.69 & No \\
    acquisition interaction (coverage) & $-0.048$ & 1.0 & No \\
    acquisition interaction (QD-score) & $-30.7$ & 1.0 & No \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{Supplementary after-generation CPU pairs @ 32{,}500 proposals ($n{=}10$): genetic vs.\ genetic\_filter on dungeon and maze. Matched-eval AUC is the H2 axis; terminal coverage saturates on both domains.}
  \label{tab:h2-cpu-full}
  \label{tab:b4-fullcpu}
  \label{tab:b5-fullcpu}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llrrrr@{}}
    \toprule
    Domain & Arm & Cov.\ (\%) & Mean evals & Skip (\%) & Cov.\ AUC $\Delta$ \\
    \midrule
    Dungeon & genetic & $89.42 \pm 0.77$ & 32{,}500 & 0 & --- \\
    Dungeon & genetic\_filter & $88.79 \pm 1.75$ & 22{,}480 & 30.8 & $+3.6$~pp @ 22{,}155 (9/10; Holm) \\
    Maze & genetic & $99.62 \pm 1.19$ & 32{,}500 & 0 & --- \\
    Maze & genetic\_filter & $99.64 \pm 1.12$ & 23{,}614 & 27.3 & $+4.6$~pp @ 23{,}300 (10/10; Holm) \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\section{Compose-gate diagnosis and gray-zone H3}
\label{sec:h3-appendix}

\emph{Not required to follow the main H1/H2 argument.} Main-text \S\ref{sec:h3} reports production \texttt{filter} levels and the gray-zone NI conclusion. This appendix records levels, the NI table, and the offline diagnosis that motivates the repair.

\begin{table}[!htbp]
  \centering
  \caption{Locked production \texttt{filter} vs.\ \texttt{hints} ($n{=}10$; descriptive only---D1 block).}
  \label{tab:h3-prod-levels}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrr@{}}
    \toprule
    Arm & Coverage (\%) & Skip (\%) \\
    \midrule
    \texttt{hints} & $60.41 \pm 0.82$ & $0$ \\
    \texttt{filter} (prod.) & $59.27 \pm 0.79$ & $33.5$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{H3-gray: gray-zone filter $-$ hints ($n{=}10$; status \textbf{NI-pass-not-superiority}; Holm $m{=}3$).}
  \label{tab:h3-gray-holm}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llrrcc@{}}
    \toprule
    Test & Mean $\Delta$ & Raw $p$ & A$_{12}$ & Holm & Detail \\
    \midrule
    Eval $\downarrow$ & $-3{,}867$ sims & $9.8\times10^{-4}$ & 1.00 & Yes & 10/10 fewer \\
    Cov NI ($>-3$ pp) & $-0.60$ pp & $1.0\times10^{-4}$ & 0.20 & Yes & 10/10 per-seed \\
    Fit NI ($>-5\%$ rel.) & $-0.36\%$ & $1.6\times10^{-5}$ & 0.40 & Yes & NI accept \\
    \bottomrule
  \end{tabular}%
  }
\end{table}


\paragraph{Dual compose definition.}
Hard fitness recompose zeros a candidate when predicted $p_{\mathrm{ext}}\ge$ an extinction gate. Live filter used gate~$0.95$ while an earlier hold-out path used $0.5$; hold-out is now aligned to runtime (\S\ref{sec:checkpoint-appendix}). The locked skip-agreement metric compares skip decisions under gates $0.5$ vs.\ $0.95$ at $\tau{=}0.45$ ($d{=}0.528$). On logged production \texttt{filter} proposals ($n{=}10$; $325{,}000$ slots/seed), $74.6\%$ of proposals fall in $p_{\mathrm{ext}}\in[0.5,0.95)$, where those gates disagree; outside that band, $d{=}0$. In-band, about $71\%$ of proposals flip skip/accept at $\tau{=}0.45$.

\paragraph{$\tau\times$emitter replay.}
Replaying the same logs stratified by emitter at $\tau\in\{0.35,0.45,0.55\}$ shows skip rates rising monotonically with $\tau$ for every emitter, and $d$ also changes with $\tau$ (Table~\ref{tab:h3-compose-tau}). At production $\tau{=}0.45$, $d$ remains high ($0.51$--$0.58$). None of those operating-band $\tau$ values brings $d$ below $0.05$; $d{<}0.05$ appears only at extreme $\tau$ (${>}87\%$ skip)---artifactual agreement, not compose repair. Blocked production H3 is therefore attributed to the extinction-gate mismatch, not a single mis-set fitness threshold.

\begin{table}[!htbp]
  \centering
  \caption{Offline replay on production \texttt{filter} proposals ($n{=}10$): skip rate (all emitters) and compose-gate divergent skip $d$ (extinction 0.5 vs.\ 0.95) at each $\tau$.}
  \label{tab:h3-compose-tau}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrrrrr@{}}
    \toprule
    $\tau$ & Skip (all) & $d$ pooled & $d$ LLM & $d$ genetic & $d$ random \\
    \midrule
    0.35 & $24.4\%$ & $0.619$ & $0.626$ & $0.580$ & $0.654$ \\
    0.45 & $33.5\%$ & $0.528$ & $0.579$ & $0.523$ & $0.508$ \\
    0.55 & $51.2\%$ & $0.351$ & $0.468$ & $0.408$ & $0.236$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\paragraph{Fix candidate and live repair.}
Offline replay confirms out-of-band proposals have $d{=}0$; force-eval of the gray zone drives $d\!\rightarrow\!0$ but cuts skip from $33.5\%$ to ${\sim}11.7\%$ on replay. The live confirmatory repair ($n{=}10$) matches that skip band and passes the NI family in Table~\ref{tab:h3-gray-holm} (mean $\Delta\text{cov}=-0.60$~pp); an exploratory duplicate replicated directionally ($-0.52$~pp). Passing the repair family does \textbf{not} rehabilitate historical production \texttt{filter} at 33.5\% skip. Repository paths for replay JSON and analysis outputs are listed with the protocol files.

\section{Primary evaluator details}
\label{sec:substrate-appendix}

Main-text \S\ref{sec:substrate} sketches genome, simulator, and behaviour axes. This appendix records fitness composition algebra and a computational edit-anatomy case study (LLM vs.\ genetic parent$\rightarrow$child diffs)---not domain claims.

\paragraph{Genome and evaluation.}
Emitters search a 21-D encoding (18 birth/survival rule bits + noise / resource\_regen / predation). Each evaluation runs a fixed $50\times50$, $T_{\mathrm{sim}}{=}200$ toroidal grid; a canonical SHA-256 seed of the genome makes fitness/BC a deterministic function of the genotype. Early extinction yields archive fitness~0.

\paragraph{Behaviour and fitness.}
Archive axes are stability and diversity in $[0,1]$. Authoritative insertion fitness recomposes diversity, survival, oscillation, and topology proxies. All fitness components below are dimensionless scores already scaled to $[0,1]$ before weighting (topology indices TI/TH are likewise clipped into $[0,1]$ before averaging):
\begin{equation}
  E_{\mathrm{ext}} = \mathrm{clip}(1 - \rho_{\mathrm{final}}, 0, 1), \quad
  T_{\mathrm{topo}} = \mathrm{clip}\!\left(\tfrac{\text{TI} + \text{TH}}{2}, 0, 1\right)
\end{equation}
\begin{equation}
  \label{eq:fitness}
  \text{fitness} = \mathrm{clip}\bigl(
    0.45\,D + 0.25\,(1 - E_{\mathrm{ext}}) + 0.20\,A + 0.10\,T_{\mathrm{topo}}
  ,\; 0,\; 1\bigr)
\end{equation}
where $D\in[0,1]$ is clipped diversity, $A\in[0,1]$ is \texttt{oscillation\_score}, and $T_{\mathrm{topo}}\in[0,1]$ is the topology score (distinct from the simulator horizon $T_{\mathrm{sim}}{=}200$ above). The outer $\mathrm{clip}(\cdot,0,1)$ is defensive. The surrogate recomposes the same formula from predicted components (\S\ref{sec:surrogate-method}). QD-score is
\begin{equation}
  \mathrm{QD} \;=\; \sum_{c \in \mathrm{filled}} f_c \;=\; |\mathrm{filled}| \cdot \bar f.
  \label{eq:qd}
\end{equation}

\paragraph{Edit anatomy (computational case study).}
\label{sec:edit-anatomy}
On the primary \texttt{hints} arm (seeds~0--9) we compare parent$\rightarrow$child genome diffs among accepted archive elites with a resolvable parent (LLM $n{=}5944$; genetic $n{=}9429$). Metrics: rule Hamming over the 18 birth/survival bits; scalar L1 on $(\texttt{noise},\texttt{resource\_regen},\texttt{predation})$. Per-seed means (Table~\ref{tab:edit-anatomy}): LLM edits are more compact on both axes (Wilcoxon one-sided less on seed-level means, $p{=}9.8\times10^{-4}$ each). Figure~\ref{fig:edit-anatomy} shows pooled Hamming histograms. Compact LLM phrases from seed~4 include \texttt{drop birth 1}, \texttt{add survival 8}, and \texttt{add birth 7} with small scalar nudges. Descriptive only---not confirmatory Holm and not a human preference study.

\begin{table}[!htbp]
  \centering
  \caption{Edit anatomy on primary \texttt{hints} ($n{=}10$ seeds; mean$\pm$SD of per-seed means among accepted elites with resolvable parent).}
  \label{tab:edit-anatomy}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrr@{}}
    \toprule
    Metric & LLM & Genetic \\
    \midrule
    Mean rule Hamming & $1.61\pm0.15$ & $2.86\pm0.08$ \\
    Frac.\ rule Hamming $\le 1$ & $56.2\%$ & $25.0\%$ \\
    Frac.\ rule Hamming $\le 2$ & $72.7\%$ & $46.8\%$ \\
    Frac.\ scalar-only (0 rule bits) & $44.7\%$ & $7.9\%$ \\
    Mean scalar L1 & $0.050\pm0.004$ & $0.112\pm0.011$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{fig_edit_anatomy_hamming.pdf}
  \caption{Rule-Hamming histograms for accepted LLM vs.\ genetic elites with resolvable parents (primary \texttt{hints}, seeds~0--9 pooled). Dashed line: mean. LLM mass concentrates at 0--2 bit flips; genetic at $\sim$3.}
  \label{fig:edit-anatomy}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.82\textheight,keepaspectratio]{fig02_elite_worlds.png}
  \caption{Example elites from \texttt{hints}, seed~4 (\texttt{q1-full}): high, median, and low fitness in the 50$\times$50 behaviour archive (10th / 50th / max rank among filled cells; fitness labels from collapsed archive, panels re-simulated via \texttt{run\_world}). Each row shows life--food boundary, 2$\times$2 heterogeneity, and food-neighbour panels for one elite.}
  \label{fig:elites}
\end{figure}

\section{Condition label map}
\label{sec:arm-map}

This appendix maps internal scheduler / artifact labels (left) to the plain names used in prose, so readers need not track repository arm strings.

\begin{table}[!htbp]
  \centering
  \caption{Arm-label map (appendix).}
  \label{tab:arm-map}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llp{5.8cm}@{}}
    \toprule
    Label & Plain name & Meaning \\
    \midrule
    \texttt{stub} & LLM + constant placeholders & Historical control; \texttt{min\_fitness\_frontier} \\
    \texttt{hints} & LLM + live scalar hints & Bundled treatment; \texttt{uniform\_frontier} \\
    \texttt{stub\_uniform} & LLM + constants (matched policy) & Same target selection as \texttt{hints}; surrogate off \\
    \texttt{filter} & LLM + scalars + acquisition & Skip low-acquisition proposals \\
    \texttt{genetic\_me\_uniform} & Genetic ME (matched policy) & No LLM; \texttt{uniform\_frontier} \\
    \texttt{genetic\_me\_filter} & Genetic ME + acquisition & No LLM; threshold filter \\
    \texttt{vanilla} & Random-only ME & 50$\times$ random emitters \\
    \texttt{cma\_me} / \texttt{cma\_mae} & pyribs CMA-ME / CMA-MAE & Continuous ES baselines \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\section{Contrast outcomes (appendix)}
\label{sec:protocol-crosswalk}

Main-text H1--H5 statuses are only Table~\ref{tab:h-status}. Table~\ref{tab:families} adds Holm width $m$ and non-H bundled checks; it does \emph{not} redefine those statuses.

\begin{table}[!htbp]
  \centering
  \caption{Locked contrasts: Holm width $m$ and status (H1--H5 copy Table~\ref{tab:h-status}; do not re-interpret). $^\dagger$ = stacked / confounded \texttt{target\_selection} (not matched H1). $^\ddagger$ = appendix / journal-extension package (not required for main-text H5).}
  \label{tab:rq-map}
  \label{tab:families}
  \small
  \fitcenter{%
  \begin{tabular}{@{}llp{2.8cm}cl@{}}
    \toprule
    Label & Locked contrast & $m$ & Status \\
    \midrule
    H1 & \texttt{stub\_uniform} vs.\ \texttt{hints} & --- & TOST-accept \\
    H2 & \texttt{genetic\_me\_uniform} vs.\ \texttt{genetic\_me\_filter} & --- & pass (eval-indexed) \\
    H3-prod & \texttt{hints} vs.\ production \texttt{filter} & --- & blocked \\
    H3-gray & \texttt{hints} vs.\ gray-zone filter & 3 & NI-pass-not-superiority \\
    H4 & hints $>$ cma\_me \textbf{and} cma\_mae & 4 & fail \\
    H5 (main) & maze AUC family & 8 & partial \\
    H5-dungeon$^\ddagger$ & dungeon AUC family & 8 & partial (appendix) \\
    Shared-checkpoint & Hold-out $R^2$/MAE & --- & pass (validity) \\
    Bundled confound$^\dagger$ & hints $>$ stub & 2 & pass$^\dagger$ (not H1) \\
    Bundled floor$^\dagger$ & hints $>$ vanilla & 2 & pass$^\dagger$ \\
    Multi-provider$^\dagger$ & hints $>$ stub \emph{per} LLM & $2$/model & pass$^\dagger$ \\
    Geometry & Grid vs.\ CVT & --- & descriptive \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\begin{table}[!htbp]
  \centering
  \caption{Paper-wide Holm robustness ($\alpha{=}0.05$, $m{=}20$): one step-down over all locked confirmatory raw $p$-values. Primary claims remain within-set (Table~\ref{tab:families}). Within-set and paper-wide reject columns agree on every row that drives a pass/fail outcome; dungeon hints$-$stub AUC tests remain non-rejects under both corrections.}
  \label{tab:paper-holm}
  \scriptsize
  \fitcenter{%
  \begin{tabular}{@{}llp{4.6cm}rcc@{}}
    \toprule
    Label & Contrast & Raw $p$ & Within-set & Paper-wide \\
    \midrule
    bundled stack & hints $>$ stub coverage & $9.8\times10^{-4}$ & Yes & Yes \\
    bundled stack & hints $>$ stub fitness & $9.8\times10^{-4}$ & Yes & Yes \\
    bundled floor & hints $>$ vanilla coverage & $9.8\times10^{-4}$ & Yes & Yes \\
    bundled floor & hints $>$ vanilla fitness & $9.8\times10^{-4}$ & Yes & Yes \\
    H4 & hints $>$ cma\_mae fitness & $9.8\times10^{-4}$ & Yes & Yes \\
    H4 & hints $>$ cma\_mae coverage & $0.50$ & No & No \\
    H4 & hints $>$ cma\_me coverage & $1.0$ & No & No \\
    H4 & hints $>$ cma\_me fitness & $1.0$ & No & No \\
    H5 dungeon & genetic\_filter $-$ genetic cov.\ AUC & $9.8\times10^{-4}$ & Yes & Yes \\
    H5 dungeon & genetic\_filter $-$ genetic QD AUC & $9.8\times10^{-4}$ & Yes & Yes \\
    H5 dungeon & hints $-$ stub QD AUC & $0.019$ & No & No \\
    H5 dungeon & hints $-$ stub cov.\ AUC & $0.032$ & No & No \\
    H5 dungeon & LLM filter $-$ hints cov.\ AUC & $0.50$ & No & No \\
    H5 dungeon & LLM filter $-$ hints QD AUC & $0.69$ & No & No \\
    H5 dungeon & acquisition interaction cov.\ AUC & $1.0$ & No & No \\
    H5 dungeon & acquisition interaction QD AUC & $1.0$ & No & No \\
    multi-prov.\ DeepSeek & hints $>$ stub coverage & $9.8\times10^{-4}$ & Yes & Yes \\
    multi-prov.\ DeepSeek & hints $>$ stub fitness & $9.8\times10^{-4}$ & Yes & Yes \\
    multi-prov.\ gpt-4o-mini & hints $>$ stub coverage & $9.8\times10^{-4}$ & Yes & Yes \\
    multi-prov.\ gpt-4o-mini & hints $>$ stub fitness & $2.9\times10^{-3}$ & Yes & Yes \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\noindent\textit{Readout:} $11/20$ paper-wide rejects. Bundled stack, floor, multi-provider, and dungeon H2 AUC transfers still reject after the global correction; H4 and the non-transfer dungeon contrasts remain non-rejects.
\FloatBarrier

\section{Per-seed paired deltas (bundled stack)}
\label{sec:rq1-seeds-appendix}

Table~\ref{tab:rq1-seeds} expands the bundled stub$\rightarrow$hints stack contrast (main-text Table~\ref{tab:rq1}) to per-seed coverage and fitness levels; it is archival detail, not a separate confirmatory family.

\begin{table}[!htbp]
  \centering
  \caption{Bundled stack per-seed levels and paired hints $-$ stub ($n=10$; same contrast as Table~\ref{tab:rq1}).}
  \label{tab:rq1-seeds}
  \small
  \fitcenter{%
  \begin{tabular}{@{}rrrrrrr@{}}
    \toprule
    Seed & Stub cov (\%) & Hints cov (\%) & $\Delta$cov & Stub fit & Hints fit & $\Delta$fit \\
    \midrule
    0 & 43.72 & 59.72 & $+16.00$ & 0.446 & 0.498 & $+0.052$ \\
    1 & 46.08 & 59.24 & $+13.16$ & 0.455 & 0.500 & $+0.046$ \\
    2 & 42.88 & 61.00 & $+18.12$ & 0.438 & 0.488 & $+0.050$ \\
    3 & 44.32 & 61.60 & $+17.28$ & 0.449 & 0.506 & $+0.057$ \\
    4 & 43.36 & 60.48 & $+17.12$ & 0.443 & 0.501 & $+0.059$ \\
    5 & 44.24 & 60.52 & $+16.28$ & 0.445 & 0.498 & $+0.054$ \\
    6 & 50.60 & 61.16 & $+10.56$ & 0.477 & 0.505 & $+0.028$ \\
    7 & 44.56 & 59.12 & $+14.56$ & 0.449 & 0.493 & $+0.044$ \\
    8 & 48.48 & 60.80 & $+12.32$ & 0.468 & 0.498 & $+0.030$ \\
    9 & 44.92 & 60.44 & $+15.52$ & 0.455 & 0.501 & $+0.046$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\section{Protocol freeze kinds and commit$\leftrightarrow$run timeline}
\label{sec:amendments-appendix}

\emph{Not required to follow the main argument.} This appendix answers a fair integrity question: with several dated protocol rows and no external OSF/Zenodo stamp, which decisions were freezes of confirmatory families, which were new packages, and which were reporting-only? We use three kinds:

\begin{itemize}
  \item \textbf{Lock} --- freezes a confirmatory family's endpoints / Holm (or NI) rules relative to that family's analysis window.
  \item \textbf{Extension} --- adds a new package or repair path; does not rewrite already-frozen families.
  \item \textbf{Reporting} --- records an outcome status, descriptive case study, or post-hoc TOST on already-collected paired seeds; not a change of locked pass/fail rules.
\end{itemize}

Table~\ref{tab:amendments} reclassifies the former flat amendment list. Confirmatory Holm families for the bundled stack, H2, H4, and dungeon AUC were not redefined by Extension/Reporting rows. Full dual-report wording lives in the repository protocol files (\texttt{EXPERIMENT\_PROTOCOL\_Q1\_v\{2--5\}.md}).

\begin{table}[!htbp]
  \centering
  \caption{Protocol chronology by kind (appendix summary). Lock = freeze of a confirmatory family; Extension = new package/path; Reporting = status or descriptive/post-hoc analysis only.}
  \label{tab:amendments}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lllp{4.6cm}@{}}
    \toprule
    Date & Kind & Scope & Note \\
    \midrule
    2026-07-12 & Lock & Primary-grid §§1--4 / F-RQ0 / F-RQ4 & Document-dated freeze (v3 header) \\
    2026-07-17 & Lock & Matched H2 genetic filter; dungeon v4 & Bundled stack / H4 wording unchanged \\
    2026-07-22 & Reporting & No external pre-registration declared & Disclosure, not a family rewrite \\
    2026-07-22 & Extension & Multi-provider bundled $n{=}10$ & Per-provider Holm; not matched H1 \\
    2026-07-28 & Extension & Hold-out gate$=$0.95; F-RQ3-gray path & Production H3 remains blocked \\
    2026-07-29 & Extension & Gray-zone exploratory pilot ($n{=}10$) & Not pooled into production H3 \\
    2026-07-30 & Extension & Maze H5 confirmatory; Sphere H2; cost wall & Maze family partial; wall descriptive \\
    2026-07-31 & Reporting & H3-gray status; edit-anatomy; matched H1 TOST & Outcome/descriptive; H3-prod still blocked \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\paragraph{Commit$\leftrightarrow$artifact timeline (claim-driving families).}
Table~\ref{tab:timeline} pairs each main confirmatory package with (i)~the protocol freeze date stated in the repository protocol file, (ii)~the observed artifact modification window under \texttt{artifacts/experiments/}, and (iii)~a representative git commit that records the corresponding protocol/analysis text in the public history. Artifact windows are filesystem mtimes (local), not wall-clock run logs; they bound when outputs were written, not a claim of OSF-style pre-registration. Where the document freeze date precedes the first git commit that carries that freeze text, we report both---the gap is exactly why an external snapshot would strengthen a later revision.

\begin{table}[!htbp]
  \centering
  \caption{Freeze vs.\ artifact window for claim-driving families (supplementary integrity table). Dates are \texttt{YYYY-MM-DD} (local). Git SHAs are short tips that contain the relevant protocol/analysis update, not necessarily the first byte of every seed archive.}
  \label{tab:timeline}
  \scriptsize
  \fitcenter{%
  \begin{tabular}{@{}llp{2.4cm}p{2.6cm}p{3.0cm}@{}}
    \toprule
    Family & Protocol freeze (doc) & Artifact window & Git anchor (protocol/analysis) \\
    \midrule
    Bundled stub/hints & v2 matrix (pre-v3) & \texttt{q1-full}: 06-29$\ldots$07-28 & historical tier; see repo \\
    H4 (F-RQ4) & 2026-07-12 & \texttt{q1-v3-pyribs}: 07-12$\ldots$07-13 & v3 freeze text in repo history \\
    H2 genetic filter & 2026-07-17 & uniform/filter: 07-16$\ldots$07-17 & matched-H2 / v4 dungeon window \\
    Dungeon AUC (app.) & 2026-07-17 (v4) & \texttt{q1-v4-dungeon-rerun}: 07-17$\ldots$07-22 & v4 lock + rerun tier \\
    Maze H5 (F-B5) & v5 family before readout & \texttt{q1-v5-maze-full}: 07-24$\ldots$07-30 & \texttt{0d28707} (07-30) \\
    H3-gray (F-RQ3-gray) & path 2026-07-28 & confirmatory: 07-30$\ldots$07-31 & \texttt{07d9550}/\texttt{716f7c3} \\
    Matched H1 TOST & Reporting (not Holm lock) & stub\_uniform / LLM tiers (prior) & post-hoc on collected seeds \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\noindent\textit{Reading rule.} A Lock row plus an artifact window that starts at or after the freeze date supports ``rules before that family's analysis.'' An Extension row must not be read as reopening H2/H4. A Reporting row (including 2026-07-31 status wording) must not be read as a new confirmatory family.

\section{CVT archive sensitivity}
\label{sec:cvt-sensitivity}

\emph{Not required to follow the main argument.} The scientific object is the attachment-point split on the confirmatory $50\times50$ grid, not archive geometry. A CVT sensitivity run ($n{=}10$) checks that bundled stub/hints/filter directions are not grid-specific (Table~\ref{tab:cvt}): concordance 10/10 on the bundled coverage lift; evaluation reduction ${\sim}31\%$ on CVT vs.\ $33.5\%$ on grid. Absolute $\Delta$coverage is larger on CVT mainly because the stub baseline is lower. These runs are descriptive and outside confirmatory Holm sets.

\begin{table}[!htbp]
  \centering
  \caption{CVT sensitivity only (paired $n=10$; not confirmatory).}
  \label{tab:cvt}
  \small
  \fitcenter{%
  \begin{tabular}{@{}lrrrr@{}}
    \toprule
    Archive & H1-s $\Delta$cov (pp) & H1-s sign & H3-s eval$\downarrow$ & Stub cov (\%) \\
    \midrule
    Grid (confirmatory) & $+15.09 \pm 2.42$ & 10/10 & 33.5\% & $45.3 \pm 2.4$ \\
    CVT (sensitivity) & $+21.51 \pm 0.79$ & 10/10 & 30.8\% & $37.4 \pm 0.4$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\section{Archive heatmap panel (supplemental)}
\label{sec:heatmap-panel}

\emph{Not required beyond main-text Figure~\ref{fig:heatmaps}.} Figure~\ref{fig:heatmap-panel} shows paired hints vs.\ CMA-ME archive fitness heatmaps for seeds~1, 4, and 6 (collapsed warm-start archives; same colour scale and layout as the main-text panel). Seed~4 is the main-text case ($\Delta$cov $-20.0$~pp); seeds~1 and~6 illustrate smaller and mid-large paired gaps ($-9.4$ / $-16.0$~pp).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{fig07_archive_heatmaps_panel_seeds1_4_6.pdf}
  \caption{Supplemental archive heatmaps: hints vs.\ CMA-ME for paired seeds~1, 4, and~6 (\texttt{q1-full} / \texttt{q1-v3-pyribs}). Rows = seed; columns = hints (left) and CMA-ME (right).}
  \label{fig:heatmap-panel}
\end{figure}


\end{document}
